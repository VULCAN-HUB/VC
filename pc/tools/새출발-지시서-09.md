# v0.1.87 — **시험표.** 잡일은 VC 가 재고, 너는 판단한다

```
VC-v0.1.87.zip
SHA-256  04BAD011094DBF7E8765A5D3E494A5C95FC8013F3015470055CAA569E3A19D04
```

**오늘부터 일하는 틀이 바뀐다.** 오너가 네 크레딧이 판단에 쓰이게 하라고 정했다.

★ **공유폴더를 비웠다.** 앞 지시서·보고서·판·모델은 만든 쪽 D 하드에 해시 대조까지 해서
옮겨 두었다(30개 일치). 모델(gguf)은 네 exe 옆 `models\` 에 이미 있는 것을 쓴다.
`fixtures-realistic\` 은 이번 지시서에 쓰므로 다시 올렸다.

★ **네 물음(v0.1.86 모델경로) 답: 설정은 손대지 마라. 이 판에서 고쳤다.**
설정에 적힌 자리에 gguf 가 없으면 exe 옆 `models\` 를 본다. 판을 딴 폴더로 옮겨
설정에 옛 자리가 남아 있어도 된다. 창 엔진 줄이 「모델 N개 대기」로 바뀌는지가 잴 값이다.
휴지통 결과 잘 받았다 — 전부 통과로 닫았다(`현재상태.md`).

```
★ 공유폴더에 새로 둔 두 장을 먼저 읽어라
   AGENTS.md    늘 같은 규칙 (이제 지시서에 안 되풀이한다)
   현재상태.md   지금 판 · 열린 문제 · 안 바뀔 값
   → 앞 지시서(01~08)는 이제 안 읽어도 된다
```

---

# 1. 새로 든 것

```
VC.exe --시험표 [폴더] [--쓴다]
   --check · --doctor · 흡수(세기 → 쓰기 → 다시 세기) · 휴지통 · 사본치우기 · 판올리기
   · 이음선 · AI 꺼내기 1·2단 · 화면(진짜 창) 을 한 번에 재서 「★시험표-v판.md」 한 장으로 적는다
VC.exe --화면상태
   진짜 창을 띄워 창 제목 · 최소 크기 · 엔진 줄 · 원격 줄 · 위/아래 띠 · 항목 수 · 메모리를
   vc-화면상태.json 에 적고 스스로 닫는다 (시험표가 부른다)
```

## 만든 쪽에서 구운 판으로 잰 것

[잰 것, 만든 PC · 구운 v0.1.87 · 빈 창고 · fixtures 6장 · gguf 는 exe 옆 models\] 93초, ✘ 0.

```
흡수   세기 남긴 3·버린 5 (38초) → --쓴다 3개 씀·휴지통 5 → 다시 세기 후보 0·겹침 9 (0.3초)
AI     1단 283자 · body 없음 / 한 편 625자 / full=1 854자
       2단 한 토막은 「-」 — fixtures 를 흡수하면 긴 글이 소제목별로 쪼개져 소제목 있는 글이 없다
화면   최소 1078x430 · 491MB · 최상위 창 1개 · 엔진 「local · 모델 1개 대기」 · 서버 전화 탈 없음
```

★ **네 자리 값이 이것과 다르면 그게 보고 거리다.** 같으면 「같다」 한 줄이면 된다.
※ 장문 다섯 값(356·849…)은 **네 장문 원본 창고**에서만 나온다 — 거기서 시험표를 한 번 더
  돌리면 2단 한 토막 줄이 찬다(폴더 없이 `--시험표` 만).

# 2. 할 것

## 가. 늘 같은 폴더로 옮기기 (이번 한 번만)
```
□ C:\VC\app 같은 한 자리를 정하고 v0.1.87 을 거기 푼다
□ 쓰던 판 옆의 models\ 와 기록자리.txt 를 그 자리 exe 옆으로 옮긴다
□ 처음 켤 때 방화벽 창이 뜨면 누르지 말고 적어라 — 다음 판부터 안 뜨는지가 이번에 잴 값이다
```

## 나. 시험표
```
□ 새 창고(빈 기록 자리)를 하나 만들어 기록자리.txt 로 가리킨다
□ --시험표 <fixtures-realistic 사본> --쓴다     ← AGENTS.md 의 명령대로
□ 표를 공유폴더에 올린다
□ ✘ 줄이 있으면 원인만 가른다
```

## 다. ★ 따로 잰다 (1~2개, 네가 고른다)
표의 어느 값이든 좋다. **내가 고르지 않는다** — 내가 고르면 내가 맞춰 둔 값만 보게 된다.

## 라. 엔진 줄 — 열린 문제 3·4
```
□ 시험표 「화면」 줄의 엔진 줄이 「모델 N개 대기」인가            ← 문제 3 고침 확인
□ 「서버에 못 물어봤다」면 같은 줄의 「서버 전화 마지막 탈」을 그대로 옮겨 적어라  ← 문제 4 실마리
```
※ 문지기 두 번 재기는 **안 해도 된다** — 네가 v0.1.84 에서 이미 쟀다(두 번 같음).

## 마. 판단 몫 — 오늘은 둘만
```
□ 긴 글 목차로 이동        (장문 원본을 넣은 창고에서)
□ [[링크]] 눌러 가기 · 뒤로
```
30분 써 보기는 판 여러 개에 한 번 한다 — 이번엔 안 한다.

# 3. 보낼 것

`★잰-것-v0.1.87-<날짜>.md` — AGENTS.md 4장의 꼴대로. **마지막 줄에 쓴 토큰.**
★ 이번이 토큰을 처음 재는 판이다. 다음 판부터 이 값과 견준다.
