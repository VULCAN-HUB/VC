# VC 현재 상태 — 판마다 만든 쪽이 고친다

## 지금 판

```
v0.1.87   VC-v0.1.87.zip   SHA-256 04BAD011094DBF7E8765A5D3E494A5C95FC8013F3015470055CAA569E3A19D04
새로 든 것  --시험표 [폴더] [--쓴다] · --화면상태
```

v0.1.87 지시서 09 는 **끝났다** (2026-09-11, 시험 PC — Codex 크레딧이 떨어져 그 PC 의 Claude 가 이어서 잼.
에이전트가 만든 쪽과 같은 종류라 「다른 방향」은 약했다. 다음 판부터 다시 Codex).

## 열린 문제

| # | 무엇 | 상태 |
|---|---|---|
| 1 | 문지기 판정이 **기계마다** 갈린다 (같은 자료·모델·판) | 오너 결정 대기 — 어떤 모델을 쓸지. 기계 사이 「어느 글이 남았나」 목록은 견주지 않는다 |
| 2 | 문지기 판정이 한 기계 안에서 갈렸다 | **소스로 돌린 첫 판만** 갈렸다(3·5 → 4·4). 구운 판끼리·네 자리는 되풀이된다. 원인 모름 — 네 자리에서 다시 잴 필요 없다 |
| 4 | 창이 서버에 못 물어보는 것 (「엔진 — 서버에 못 물어봤다」) | v0.1.87 에서 **양쪽 다 재현 안 됨**. 지켜만 본다 — 다시 나면 시험표 「화면」 줄의 「서버 전화 마지막 탈」을 옮겨 적어라 |
| 5 | 아직 못 잰 것 | **다음 판을 같은 폴더(`C:\VC\app`)에 덮어 풀고 처음 켤 때 방화벽 창이 뜨나** — 이번에 못 잰 유일한 값 · 30분 써 보기 |
| 6 | 시험표 「2단 한 토막」 3147 (안 바뀔 값 849) | **만든 쪽 도구 탓**(첫 소제목 = 글 전체 H1 을 쟀다). 네 자리 직접 측정 849·831 로 굳었다. 다음 판에서 둘째 소제목 + 이름을 적고, 색인이 빈 창고도 건너뛰지 않게 고친다 |
| 7 | 편집 중 검색칸에 친 말이 **본문에 들어가 저장됐다** · 목차 콤보 뒤 글자가 검색칸으로 안 갔다 | [만든 PC 1회 · 긴 글 안 빈 곳을 누른 뒤] 났다. [네 자리 · 짧은 글 · 세 갈래 1회씩] **재현 안 됨.** 조건이 다르다 → 만든 쪽이 긴 글에서 다시 재고 코드를 판다 |
| 8 | ★ `vc-죽음.log` 에 `Windows fatal exception: code 0x8001010d` 7회 — **창은 전부 정상 종료** | [네 자리] onnx 임베더 실에서 · 한 번은 방화벽 창이 떠 있던 4초 사이. 비치명 COM 예외를 faulthandler 가 적은 것으로 본다 [짐작이다]. 그러면 「죽음 기록이 자랐으면 무언가 죽었다」가 흐려진다 → 만든 쪽이 판다 |
| 9 | 창 검색이 **어느 글에도 없는 말**에도 「관련 N개야」 하며 항목을 다 내놓는다 | [네 자리 · 항목 4~5개 창고] 뜻 검색이 빈자리를 채우는 것으로 본다 [짐작이다]. 「관련」이라는 말이 거짓말인지 만든 쪽이 본다 |

## 닫힌 문제 (확인됨)

- 3 · 창·서버가 exe 옆 모델을 못 찾던 것 — 네 자리 두 창고 모두 「모델 1개 대기」, 설정이 옛 v0.1.82 자리를 가리켜도 찾는다
- 목차 이동 · `[[링크]]` 눌러 가기(빈칸 든 제목 포함) · ← · Alt+← — 양쪽 다 통과
- 밖에서(PowerShell) 고친 줄을 열린 카드·창 검색·API 가 받는다 (10초 뒤 확인)
- 휴지통 글은 창 검색에도 API 에도 안 나온다
- 같은 판을 같은 폴더에서 다시 켜면 방화벽 창이 안 뜬다 (방화벽 이벤트 로그로 잼)
- 휴지통 (v0.1.86): 버린 것 보관 · 다시 부을 때 문지기 안 부름 · 옮기면 되살아남

## 안 바뀔 값 (바뀌면 보고) — **무엇으로 쟀는지 같이 적는다**

```
「긴 글 검증 절차」 원본 3069자 · 전체 JSON 글자 수 · Bearer 로 서버에 직접
  note 한 편                              3226
  note 한 토막 「저장과 중복 방지」          849      (양쪽 잰 것)
  note 한 토막 「입력과 경계 조건」          831      (양쪽 잰 것 · 다음 판 시험표가 재는 것)
  search 1단 · search full=1             356 · 3457  (시험표가 고른 물음말 기준)
※ 없는 소제목 404 의 글자 수 · search 글자 수는 **물은 말 길이를 따라 바뀐다** — 물은 말이 다르면 견주지 마라
※ full=1 은 search 쪽 인자다. note 에 붙이면 무시되고 한 편이 온다
※ 네 long 창고의 VC.md 에는 Codex 가 넣은 「[[긴 글 검증 절차]]」 줄이 남아 있어 그 물음말로 찾으면 VC 가 끼어 값이 는다

--check 통과 · 기록자리.txt 를 exe 옆에서 읽음 · 씨앗이 있어도 첫 흡수가 막히지 않음
--사본치우기 · --판올리기 가 말을 한다(stdout) · 떠 있는 최상위 창은 주 창 하나
```

## 닫힌 것 — 다시 묻지 않는다

- 카드 위 줄 정렬: 지금 그대로 (오너 결정)
