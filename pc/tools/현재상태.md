# VC 현재 상태 — 판마다 만든 쪽이 고친다

## 지금 판

```
v0.1.87   VC-v0.1.87.zip   SHA-256 04BAD011094DBF7E8765A5D3E494A5C95FC8013F3015470055CAA569E3A19D04
새로 든 것  --시험표 [폴더] [--쓴다] · --화면상태
```

## 열린 문제

| # | 무엇 | 상태 |
|---|---|---|
| 1 | 문지기 판정이 **기계마다** 갈린다 (같은 자료·모델·판) | 오너 결정 대기 — 어떤 모델을 쓸지. 기계 사이 「어느 글이 남았나」 목록은 견주지 않는다 |
| 2 | ★ 문지기 판정이 **한 기계 안에서도** 갈렸다 | [잰 것, 만든 PC · 소스] fixtures 세기만 → 남긴 3·버린 5, 곧바로 --쓴다 → 4·4. [잰 것, 만든 PC · 구운 판] 두 번 다 3·5. [잰 것, 네 자리 v0.1.84] 두 번 다 3·5. → **소스로 돌린 첫 판만 갈렸다.** 구운 판끼리는 되풀이된다. 원인 모름 — 네 자리에서 다시 잴 필요는 없다 |
| 3 | 창·서버가 exe 옆 모델을 못 찾던 것 (「엔진 local · 모델 없음」) | **v0.1.87 에서 고침** — 설정에 적힌 자리에 gguf 가 없으면 exe 옆 `models` 를 본다. 설정은 손대지 마라 |
| 4 | 창이 서버에 못 물어보는 것 (「엔진 — 서버에 못 물어봤다」) | v0.1.87 전 구운 판에서 한 번 재현(서버는 밖에서 부르면 12ms 에 답함). **v0.1.87 구운 판에서는 재현 안 됨** — 엔진 줄 「모델 1개 대기」 · 서버 전화 탈 없음. 원인은 못 짚었다. 「화면」 줄에 「서버 전화 마지막 탈」이 찍히니 다시 나면 그 값이 실마리다 |
| 5 | 화면에서 못 잰 것 | 긴 글 목차로 이동 · `[[링크]]` 눌러 가기 · 뒤로 · 밖에서 고친 것 · 창 검색이 휴지통 글을 안 내놓나 · 30분 써 보기 |

## 닫힌 문제 (네가 확인함)

- 휴지통 (v0.1.86): 버린 5개 보관 · 다시 부을 때 후보 0 / 겹침 9 (0.312초, 문지기 안 부름) · API 검색에 안 나옴 · 파일을 글 폴더로 옮기면 되살아남

## 안 바뀔 값 (바뀌면 보고)

```
장문 원본 3069자 글 기준 AI 꺼내기   1단 356 · 한 토막 849 · 없는 소제목 404 · 한 편 3226 · full=1 3457
                                  (전체 JSON 글자 수. 네 창고의 「긴 글 검증 절차」 원본 기준)
--check 통과 · 기록자리.txt 를 exe 옆에서 읽음 · 씨앗이 있어도 첫 흡수가 막히지 않음
--사본치우기 · --판올리기 가 말을 한다(stdout) · 떠 있는 최상위 창은 주 창 하나
```

## 닫힌 것 — 다시 묻지 않는다

- 카드 위 줄 정렬: 지금 그대로 (오너 결정)
