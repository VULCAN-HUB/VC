# VC 현재 상태 — 판마다 만든 쪽이 고친다

## 지금 판

```
v0.5.4   VC-v0.5.4.zip (300.5MB · 풀면 579.2MB)   SHA-256 BA33B74FBAB779D8A54CF0B01D026C4E479A769EB5E12F7A00EA9745A6FB32B2
새로 든 것  동시 쓰기 줄 손실 막이 묶음 · sqlite 연결 잠금 · 설정 창(전체화면·내 정보)
[잰 것, 구운 v0.5.4 · 오너 창고 D:\옵시디언\VC 2795장]
  --doctor v0.5.4 · 뜻 왕복 ✔ · Defender 이상 없음
  --화면상태 창 뜨기 2초 · 1,573MB · 떠 있는 창 1 · 「끔 (0)」 · 죽음 기록 0줄 · 자체점검 30/30
  잣대(소스) 1단 평균 787자 · 찾음 10/20 · 한 물음 1305자 — 고치기 전과 같다
[잰 것, 구운 v0.5.4 · 실무 창고 VC-실무\기록 2794장 · 큰 모델]
  한바퀴 20물음 1단 평균 777자 · 찾음 13/20 · 전체 33,172자 · 인사 987자 — v0.5.3 과 같다
  덮어쓰기보호 409 · 사람 줄 삶 · force 201 · undo 있고 지난 판에 사람 줄 있음 · 치움 200
  창 1,565MB · 「끔 (0)」 · 죽음 기록 0
```
★ **모델을 빼고 재면 실무 창고 벡터가 그 모델 폭으로 통째로 다시 만들어진다** — 2026-09-13 메모리 A/B 로 e5-base 를 잠깐 빼고 창을 띄웠더니 384폭으로 바뀌어, 다음 소스 서버가 768 로 2794장을 다시 만들었다(몇 분). 모델을 뺀 실험은 **사본 창고**에서 한다.
★ 잴 때 `VC_DATA` 는 **`D:\옵시디언\VC`** 다(`…\VC\data` 가 아니다 — 거기 주면 빈 창고가 새로 생긴다. 한 번 그래서 치웠다).

### 맥 묶음 — 굽기 전에 모아 고친 것 (2026-09-15 · 소스 · 아직 안 구움)

- **기계 파일을 앱 자리로**(결정 16): `paths.state_dir()` · `기계자리()`.
  - 구운 판: 맥 `~/Library/Application Support/VC` · 윈도우 `%LOCALAPPDATA%\VC`. 소스로 돌면 기록 자리 그대로, `VC_DATA` 를 주면 따라간다(시험 격리).
  - 대상: db 둘 · 설정 · 테마 · 마이크 · 로그 · 죽음 · 메모리 · 화면상태 · 자전 · 진단 · 보고서들 · talk.jsonl · 녹음 · artifacts. **글 · 첨부 · 휴지통은 기록 자리.**
  - 옛 창고: 켤 때(서버 없고 잠금 잡은 뒤, db 열기 전) **복사 → 확인(db quick_check · json 읽기 · 크기) → 원본 지움**. 확인 실패면 원본을 두고 옛 자리를 계속 쓴다.
  - 잠금 파일도 앱 자리(기록 자리 지문으로 따로) · 진단 묶음 기본 자리도 앱 자리 · `--doctor`·진단에 「앱 자리」.
  - [잰 것] paths 자체점검 — 깨진 db 는 안 옮기고 원본·옛 자리 유지 · 옮긴 db 는 원본 사라짐 · 글 안 건드림 · 두 번 불러도 같음.
- **자체점검 누수 막음**: 서버를 띄우는 자체점검이 결과물 자리를 안 줘 진짜 기록 자리에 `data/artifacts/*.txt` 가 검사마다 쌓였다(33개).
  - [잰 것] 누수 검사 추가 · 막이를 빼면 phone_relay · server 둘 다 터짐 · 되돌리면 통과 · 모두검사 뒤 0개.
- **앱 — 윈도우에서 넘어온 것**: 적기 탭 알림 줄이 옛 말로 남던 것 → 대기함이 그 글을 보내면 줄도 「서버 저장 완료」로.
  - [잰 것] `app/test/write_notice_test.dart` · 고침을 끄면 터짐 · 되돌리면 통과.
- **앱**: 주소 칸에서 키보드가 「연결」을 덮던 것(`scrollPadding`) · 화면 글 「PC」 → 「PC·맥」/「컴퓨터」 11곳 · Info.plist 2곳.
- **PC**: 차림표 단축키를 맥에서 ⌘ 로 표기(`(Ctrl+,)` → `(⌘,)`).
- [잰 것] 모두검사 35/35 · flutter analyze 이상 없음 · test 12.
- **구운 VC.app ⑦ 실기에서 잡은 버그**: 설정·테마·대화 기록·녹음·마이크 자리를 **모듈 불러올 때** 박아, 옮긴 뒤 첫 켜기에 **새 열쇠로 옛 자리에 설정을 다시 만들었다**(폰 짝짓기 풀림 · 기록 폴더에 열쇠 파일). → 전부 부를 때 정하게. [잰 것] server 자체점검에 되돌리면 터지는 검사(되돌려 「열쇠가 바뀌었다」로 터짐 확인).
- [잰 것, 구운 VC.app v0.5.19 · 격리 시험 창고(VC_DATA·VC_STATE) · 옛 판 모양] ⑦ 통과
  - 처음 켜기: 기계 파일 4개 앱 자리로 · 창고 루트에 남은 것 없음 · 옮긴 eb.db quick_check ok · 옛 열쇠 그대로
  - 새 글 201 · 옛 글·새 글 검색 맨 위 · zip 을 덮어 풀고 다시 켜도 둘 다 검색됨
  - 굽기 안 자체점검 35/35 · 대화 엔진 Metal 판 담김(libggml-metal)
- 아직: 아이폰 덮어 설치(⑥) · 윈도우 D 창고 옮기기 실기.

### 2단계 맥·PC 쪽 — 폰 연결을 테일스케일로 (2026-09-15 · 소스)

- `pc/tailnet.py`: 이 컴퓨터의 테일스케일 IPv4. 테일스케일 명령(`ip -4`) → 안 되면 **터널 인터페이스(utun·tailscale·「Tailscale」 어댑터)에 붙은** 100.64.0.0/10 만. 꺼져 있으면 None.
- 설정 「폰 연결」: QR·폰 주소를 테일스케일 주소로(결정 18). 꺼져 있으면 경고 한 줄 + 따로 누르는 「집 와이파이 주소로 보이기」(결정 21 — 조용히 집 주소로 안 넘어간다).
- 웹앱·설정 안내 문구의 「같은 와이파이」 → 테일스케일.
- [잰 것] tailnet 자체점검(맥 ifconfig · 윈도우 ipconfig 꼴 · 통신사 CGNAT 착각 막기 · 대역 끝) · 막이를 되돌리면 「일반 인터페이스의 CGNAT 주소를 테일스케일로 착각한다」로 터짐 · 이 맥에서 실제 테일스케일 주소가 잡힘.
- [앱 쪽] `VcOffline` 이 까닭을 싣는다 — 연결 거절 → 「VC 가 꺼져 있다」 · 경로 없음 → 「망이 달라 닿지 않는다」 · 「시간 초과」 · 끊김. 모르면 비워 둔다(지어내지 않는다). 머리 알림: 「컴퓨터에 못 닿았어 (까닭) — 테일스케일·VC 켜짐을 봐 줘」. 짝짓기 안내·iOS 로컬 네트워크 설명도 테일스케일로.
- [잰 것] flutter analyze 0 · test 16개 통과(새 `offline_reason_test` 4개) · 까닭을 빼면 2개가 터짐.
- [⑥ 실기 2026-09-15] 맥 VC 를 끈 채 아이폰에서 글 하나 저장(22:37, 전송 대기) → 새로 구운 앱을 `devicectl device install app` 으로 덮어 설치 → 폰 앱 저장소 `Library/Application Support/vc_outbox.json` 을 꺼내 보니 3건 중 대기 1건 그대로(보낸 2건도 남음). **통과.**
- [테일스케일 실기 2026-09-15] 폰 테일스케일 로그인 → 맥 설정 QR(테일스케일 주소) 다시 찍기 → ⑥ 에서 남긴 대기 글이 「보냄」으로 바뀌고 맥 창고에 `전송대기.md` 로 들어옴. iOS 「로컬 네트워크」 허락 창은 **안 뜸**(테일스케일 주소는 로컬 네트워크로 치지 않는 듯).
- [이동통신 실기 2026-09-15] 폰 와이파이 끄고 데이터+테일스케일만. 맥 VC 끈 채 글 저장(22:52) → 대기로 남음(시도 3) → 맥 VC 켜고 앱 새로고침 → 「보냄」(시도 4) · 맥 창고에 들어옴. **집 와이파이 없이 테일스케일로 닿는다 — 통과.**
- [오너 실기] VC 를 끄고 보내니 폰에 「컴퓨터에 못 닿았어」만 떴다 — **대기함 보내기(`_send`) 길이 까닭을 버리고 있었다**(`_fail` 길만 붙였음). `Outbox.offlineReason` 으로 들고 와 알림 줄에 붙였다. `outbox_reason_test` · 빼면 터짐 · test 17개 통과.
- [오너 실기 23:30 · 5G+테일스케일] 맥 VC 끈 채 보내니 한 줄에 「컴퓨터에 못 닿았어 (VC 가 꺼져 있다) — 테일스케일·V…」 + 「보낼 것 1」. **통과.** 흠: 못 닿았는데 「창고 앞머리 0장」이 떠 비어 보인다.

### 3단계 — 3단 틀 (결정 17) · 진행 중

- [폰] 늘 보이는 한 줄에 「보낼 것 N」(없으면 안 보임) — 연결 점 · 까닭 붙은 상태 글 옆. `home_line_test`(까닭 + 보낼 것 수) · 빼면 터짐 · test 18개 통과.
- [맥·PC] 아래 띠 = 늘 보이는 한 줄: 오류(이름 겹침) · 준비 중(뜻 익히는 중) · 밖에서 닿게 열렸는데 테일스케일이 꺼지면 「⚠ 테일스케일 꺼짐 — 폰이 밖에서 못 닿는다」. 아무 일 없으면 비움. 센 값(적은 것 · 보임 · 연결)은 오른쪽 칸 「// 상태·기록 +」를 눌러야 보인다. 테일스케일은 딴 실에서 30초마다 본다(창이 안 굳게).
- [잰 것] ui_check 에 한 줄/펼침 분리 · 폰 길 경고(닫혀 있으면 안 함) · 경고 줄을 빼면 터짐 · 모두검사 36개 통과.
- 아직: 설정 「한 곳+VC 화면 / 둘 다」 · 실제 창에서 눈으로 보기.

### 첫 버전 목표 — 「기록을 믿고 맡길 수 있는 맥·아이폰」 (오너 확정 2026-09-14)

범위는 **새 글 작성의 오프라인 저장·전송**이다. 같은 글을 양쪽에서 고치는 동기화, 대화·음성·자동 학습은 코드는 두고 새로 만들지 않는다.

- **서버**(`store.client_writes` · `POST memory` 의 `client_id`)
  - 같은 id 는 한 번만 받는다. 받은 표시는 eb.db 에 남는다.
  - 「쓰기 시작」 표시만 남고 꺼졌으면, 글에 그 몸이 있는지 보고 가른다.
- **앱**(`app/lib/outbox.dart`)
  - 저장을 누르면 **폰 파일에 먼저** 넣는다(임시 파일에 쓰고 이름 바꾸기).
  - 앱 열기 · 앞으로 돌아오기 · 저장 직후 · 「지금 보내기」 때 오래된 것부터 보낸다.
  - 상태 셋을 보인다: **폰에 저장됨 / 전송 대기 / 서버 저장 완료**.
  - 401 이면 글은 두고 짝만 푼다. 깨진 대기함 파일은 옆에 치운다.

[PC 에서 확인한 것]
- 서버(phone_app 자체점검)
  - 같은 id 재전송 → 200 duplicate · 한 줄
  - **서버 재시작 뒤** 재전송 → duplicate · 한 줄
  - 쓰다 꺼짐(시작 표시 + 글에 있음) → 안 붙음
  - 시작 표시만 있고 글엔 없음 → 씀
  - 같은 제목 다른 id → 따로 붙음
  - 틀린 id → 400
  - 막이를 되돌리면 터진다
- 앱(flutter test 10)
  - 다시 켜도 미전송 글 남음
  - 못 닿으면 「전송 대기」로 남고 다시 켜도 그대로
  - **응답 유실 → 앱 재시작 → 재전송해도 같은 id · 서버 1건**
  - 401 이면 글 유지
  - 서버 거절 까닭 남김
  - 깨진 파일 치움
  - 보낸 기록 30개만 남김
  - 저장 화면: 못 닿아도 목록에 「전송 대기」 · 파일에 남음
- 자체점검 35/35 · analyze 이상 없음
- **안드로이드 에뮬레이터 끝까지**(APK 0.3 · 파일에 두는 임시 VC 서버)
  1. 서버 꺼진 채 짝짓기 → 새 글 저장 → 「전송 대기 1」
  2. **앱 강제 종료** → 서버 켬 → 앱 다시 켬 → 저절로 보내 「서버 저장 완료」
  3. **서버 재시작** 뒤 검색에 나옴
  4. 앱을 다시 앞으로 불러와도 본문 한 줄 그대로(겹침 없음)
  - 본 것: 보낸 뒤에도 머리글이 「창고 0장」으로 남았다(보내기 전에 셌다) → 보낸 뒤 다시 센다.
  - 본 것: 「보기」 목록도 켤 때 한 번만 불러 보낸 뒤 「0장 · 안 나왔어」가 남았다 → 대기함이 글을 보내면 다시 부른다.
  - 두 고침은 test·analyze 만 거쳤고 APK 0.3 에는 안 들어갔다(맥·아이폰에서 볼 것)

[맥·아이폰 실기로 확인한 것 — 2026-09-15 · 소스 맥 VC · 아이폰 iOS 26.6 무료 서명]
- ① 맥 VC 끈 채 폰 저장 → 「전송 대기 1」 ✔
- ② 앱 완전 종료 → 다시 켜도 「전송 대기」 그대로 ✔
- ③ 맥 VC 켬 → 「지금 보내기」 → 「서버 저장 완료」 · 맥에 파일 1개 · 검색 맨 위 ✔
- ④ 같은 client_id 두 번 → 201 뒤 200 duplicate · 본문 1번 ✔(와이파이 끊기 대신 막이를 직접 잼)
- ⑤ 맥 VC 재시작 뒤 같은 번호 → duplicate · 두 글 검색 맨 위 ✔
- 본 것: 폰이 다른 와이파이면 「PC 에 못 닿았어」 · **아이폰 미러링 중에도 못 닿음**(바깥 연결 0번)
- 시뮬레이터: `NSAllowsLocalNetworking` 만으로 `http://192.168.x.x` 에 닿는다(가짜 열쇠 → 401)
- 자체점검 35/35(맥) · flutter analyze 이상 없음 · test 11

[맥·아이폰 실기로 남은 것]
- ⑥ 앱 덮어 설치(업데이트) 뒤 대기함 · 보낸 기록이 남는지
- ~~⑦ 맥 VC 새 판을 덮어 푼 뒤 기록·검색~~ → 통과(구운 VC.app, 격리 창고)
- 「로컬 네트워크」 허락 창이 뜨는지(오너 확인 대기)

### 앱 디자인 다시 — 0.2 (오너 지시 2026-09-14 「앱 디자인과 초기화면 디자인 다시」)

```
VC-app-android-0.2.apk (68.4MB)   SHA-256 880D16F691D5AAEDE6E039BFB0C8B37655DF207DD3BDA639934EE4C948B43CF5
[잰 것, 에뮬레이터 android-36 · 임시 VC 서버] 시스템 시작 화면(검정+VC 표식) → 앱 시작 화면(달아오른 바탕·숨 쉬는 표식·「// PC 를 찾는 중」)
  → 짝짓기(순서 01~03 판·QR 찍기·주소로 직접 연결·붙을 PC 확인 창) → 보기(카드·// 창고 앞머리) → 글 펼치기 → 적기 · test 4/4 · analyze 이상 없음
```
- 얼굴은 PC 기본 테마 **불칸**(검정 · #FF3D1F · `// 구역` 이름표)에 맞췄다. 0.1 은 계기판(청록)이라 PC 와 다른 물건처럼 보였다.
- 표식·아이콘·시작 그림은 `app/tools/make_assets.py` 가 PC `logo.VulcanMark` 로 굽는다. 대상: 안드로이드 mipmap · 시작 화면(12+ 포함), iOS AppIcon · LaunchImage.
★ 에뮬레이터로 보고 고친 것:
- `'monospace'` 글꼴이 한글을 한 칸씩 벌려 「창 고 앞 머 리」가 됐다. 기본 글꼴에 숫자 폭만 고정한다.
- 머리글 표식이 링째 작게 찍혀 안 보였다. 링 없는 표식 그림을 따로 만들었다.
- Gradle 메모리 상한 8G 를 3G 로 내렸다(RAM 7.8GB).

### 아이폰·안드로이드 앱 첫 판 (`app/`, Flutter 3.47.4 — 오너 지시 2026-09-14 · 결정 13)

```
VC-app-android-0.1.apk (67.6MB)   SHA-256 42E908B225660215B9D013238BFF4F41D3F78FD4136D23AC9417F85B3D5D2234
[잰 것, 에뮬레이터 android-36 x86_64 · 임시 VC 서버(샘플 2장)]
  붙여 넣은 QR 주소 → 「이 PC 에 연결할까? 10.0.2.2:8799」 → 연결 · 창고 2장 목록 · 글 펼치기(<script> 가 글자로)
  적기(제목 비움) → PC 에 「2026-09-14」 글로 저장 확인 · flutter test 4/4 · analyze 이상 없음
```
하는 일: 보기(찾기·펼치기)·적기(같은 제목 덧붙기, 못 닿으면 글이 칸에 남음)·QR 짝짓기(열쇠는 기기 보안 저장소, 붙을 PC 주소를 사람이 확인)·연결 지우기. 401 이면 짝을 푼다.
★ 굽기 함정 넷(`app/build_apk.sh` 에 적음):
- Gradle 은 한글 경로를 거절하고, Dart 분석기도 한글 경로에서 죽는다. 정션은 소용없어 `subst V:` 로 굽는다.
- 그 V: 에서 Kotlin 증분 캐시가 깨져 `kotlin.incremental=false` 를 두었다.
- sdkmanager 둘을 동시에 부르면 0xC0000409 로 죽는다.
- Git Bash 가 `/sdcard/…` 를 윈도우 경로로 바꾸므로 adb 는 `MSYS_NO_PATHCONV=1` 로 부른다.
★ **이 PC 는 RAM 7.8GB** — 에뮬레이터 기본 메모리와 Gradle 데몬(8GB 상한)이 겹치자 페이징 파일이 모자라 셸이 통째로 멈췄다. 에뮬레이터는 `-memory 1536 -no-window` 로 띄우고, 굽기가 끝나면 Gradle 데몬을 끈다.
남음: 안드로이드 실기(오너) · 아이폰 굽기(Mac 도착 뒤, 코드·권한 문구는 준비됨) · 카메라 QR 찍기는 실기에서만 잴 수 있다

### v0.5.18 묶음 (구웠다, 2026-09-14) — 폰 앱 첫 판(오너 요청 2026-09-14)

```
v0.5.18  VC-v0.5.18.zip (300.6MB · 풀면 579.3MB)   SHA-256 551E4C4E32CC53C694269AB52A477EA835DFAE4377C8D2BB55382793B6D7E020
[잰 것, 구운 v0.5.18 · 실무 창고 VC-실무\기록 2794장 · e5-base 를 exe 옆 models 로 옮겨 둠]
  뜻 모델 목록 e5-base · 딸려 온 것 둘 다 · 쓰는 것 e5-base · Defender 이상 없음 · 자체점검 35/35
  한바퀴 1단 772자 · 찾음 13/20 · 인사 1,144자 · 덮어쓰기보호 다 됨 · 폰 앱 /app 200(열쇠 없음·CSP)·목록 20장·펼치기 200
```
★ 굽기는 **Bash 에서 `PYTHONIOENCODING=utf-8 powershell -File build.ps1`** 로 — PowerShell 도구에서 `*>&1` 로 부르면 PyInstaller stderr 가 오류로 바뀌어 멈추고, 인코딩을 안 주면 VC.spec 의 `—` 출력에서 cp949 로 죽는다

**폰 앱(결정 12)** — `phone_app.py`: PC 서버가 띄우는 웹앱 `GET /app`. 설치 없이 안드로이드·아이폰 한 벌. 오너 지시대로 첫 판은 AI 없이 **보기**(`memory/search` → `memory/note`, 첫 화면은 창고 앞머리 20장)와 **적기**(`POST memory`, 같은 제목이면 덧붙기, 못 적으면 친 글 남김)만. 짝짓기: 설정 「폰 연결」 의 「QR 보이기」 → `http://내부주소:8765/app#t=열쇠`(열쇠는 `#` 뒤라 서버로 안 감, 앱이 폰에 적고 주소에서 지움, QR 은 2분 뒤 사라짐). 막이: 페이지에 열쇠 없음 · 받은 글은 `textContent` 만(창고 글의 `<script>` 가 글자로 보임) · CSP 그대로 · 앱이 부르는 길 넷을 자체점검이 대조 · eb.py PORT 대조. 새 글은 201 — 처음엔 앱이 실패로 봤다(검사가 잡음)
[잰 것, 소스 · 임시 서버 · 폰 크기 375×812 브라우저] 열쇠 주소로 열면 주소창에서 `#t=` 가 지워지고 폰에 남음 · 창고 2장 목록 · 글 펼치기에서 `<script>` 가 글자로 나옴 · 적기 → PC 글에 덧붙음 확인 · 자체점검 35/35
남음: 폰 실기(오너) · 윈도우 방화벽이 8765 안쪽 접속을 막는지는 실기에서 본다

**★ 받은 모델이 판을 올리면 지워졌다** — 첫 v0.5.18 을 실무 폴더에 풀자 받아 둔 e5-base(`_internal\models`, 프로그램 속)가 사라져 뜻 벡터가 작은 모델 폭으로 통째로 다시 만들어지고 한바퀴 찾음 12 → **4/20**, 인사 1,247자(「e5-base 받아라」 안내가 붙음). 오너도 v0.5.17 에서 받은 뒤 새 판을 덮어 풀면 똑같이 겪는다. 고침: `paths.fetched_dir()` = 설치본은 **exe 옆 `models`**, 받기(`Downloader`)·gguf 자리가 거기로. 뜻·목소리 모델 목록(`models_config.installed`)·`meaning_dir`·목소리(`Mouth`)는 **받은 자리와 딸려 온 자리를 둘 다** 본다. 「큰 모델 받아라」 안내(인사·`--doctor`)도 exe 옆까지 본다. (gguf_dir 옛 모양·목록이 한 자리만 보기 — 되돌리면 각각 터짐 · 자체점검 35/35)
★ 이미 옛 판에서 받아 둔 경우: 새 판을 풀기 전에 `_internal\models\e5-base` 를 exe 옆 `models\e5-base` 로 옮기면 다시 안 받는다

### v0.5.17 묶음 (구웠다, 2026-09-14) — 오너 결정 4~10 추천대로 확정(2026-09-13)

```
v0.5.17  VC-v0.5.17.zip (300.6MB)   SHA-256 CC0D7F5498D2C7A6F58E20D5CBA05FF6A2390B074BE36019482EF375CE52CBB7
[잰 것, 구운 v0.5.17 · 실무 창고 VC-실무\기록 2794장 · e5-base]
  --doctor v0.5.17 · 뜻 왕복 ✔ · 창 19.4초 · 1,573MB · 「끔 (0)」 · 죽음 기록 0 · 자체점검 34/34
  한바퀴 1단 767자 · 찾음 12/20 · 인사 1,144자 · 덮어쓰기보호 다 됨 · connect/google(연결 없음) → 404
```

**Google 계정 연결(결정 8)** — `google_auth.py`: 설치형 OAuth(127.0.0.1 루프백 · PKCE · state 대조), **읽기 전용 범위만**, 새로고침 토큰·클라이언트 비밀은 보관소에만. 설정 「외부 연결」 에 클라이언트 ID·비밀 칸 + 「구글 로그인」 단추(뒤 실에서 돌고 창은 타이머로 결과만 읽는다). `connect/google?path=/drive/v3/files` · `/calendar/v3/users/me/calendarList` · `…/events` 읽기. 오너가 할 일: Google Cloud 에서 「데스크톱 앱」 클라이언트 만들기 (state 막이 되돌리면 터짐 · 검사는 가짜 브라우저·가짜 토큰 교환)

**VC 가 스스로 대화에서 오너 정보를 짐작(결정 9)** — `selflearn.py`. 로컬 대화 모델이 있고 백엔드가 자체 엔진일 때만, 주기 분석(15분) 뒤 **새 대화의 오너 말만** 로컬 모델에 주고 JSON 으로 받는다. 막이: 짐작으로만(`learn` — 오너 답 안 덮음) · 질문 목록 안의 질문만 · **답 낱말이 오너가 실제로 한 말에 있어야**(지어낸 것 버림) · 한 번에 5개 · 본 줄은 다시 안 읽음(`vc-스스로짐작.txt`). 이 PC 밖으로 안 나간다 (지어낸 것 막이·확인 물음 되돌리면 각각 터짐)
[잰 것, 진짜 qwen2.5-1.5b · 말 넷] 질문 목록을 통째로 주면 늘 `[]` → 질문 6개씩 「질문: 답」 객체로 묻게 바꿨다. 낱말만 겹치면 엉뚱한 짝도 넘어 **짝마다 예/아니오 확인**을 더했다. 그래도 후보 5개 중 **맞음 3**(호칭·업무·불리고 싶은 이름) · **틀림 2**(맡길 일: 점심 냉면 · 시간대: 점심). 더 엄격한 확인 물음 두 안은 맞는 것까지 전부 아니오(2/5) — 1.5B 의 한계다(열린 문제 20). 짐작은 질문 창에서 오너가 확인해야 확정이라 틀을 유지한다

### v0.5.16 묶음 (구웠다, 2026-09-13)

```
v0.5.16  VC-v0.5.16.zip (300.6MB)   SHA-256 7FD0F983E97D56F975601C65BFFA73A735B756E2050CC19C08D78300F5DB6882
[잰 것, 구운 v0.5.16 · 실무 창고 VC-실무\기록 2794장 · e5-base]
  --doctor v0.5.16 · 뜻 왕복 ✔ · 창 18.9초 · 1,568MB · 「끔 (0)」 · 죽음 기록 0 · 자체점검 32/32
  한바퀴 1단 767자 · 찾음 12/20 · 인사 1,144자(★상한 1,200 에 가깝다 — 안내를 더 늘리려면 줄일 곳부터) · 덮어쓰기보호 다 됨
  connect/github(연결 없음) → 404
```

**연결한 계정에서 읽기만 — `GET /eb/v1/connect/<github|notion>?path=`** (`connectors.py`) — 허용 길(정규식)만 부르고 GitHub 는 GET, Notion 은 읽기용 `POST /search` 만. 토큰은 보관소에서 꺼내 요청 머리에만(답·오류에 안 실림, 오류 본문의 토큰도 가림) · 2만 자 넘으면 `cut` · 원격 403 · hello 안내에 한 줄 (검사: 가짜 네트워크·가짜 보관소 — 진짜 계정은 안 부른다)

### v0.5.15 묶음 (구웠다, 2026-09-13) — 오너를 자세히 알기(오너 요청)

```
v0.5.15  VC-v0.5.15.zip (300.6MB)   SHA-256 0948BEBABA574878F1C6C74964D08CA5FB7F770ABA11FF41D389C7E519938264
[잰 것, 구운 v0.5.15 · 실무 창고 VC-실무\기록 2794장 · e5-base]
  --doctor v0.5.15 · 뜻 왕복 ✔ · 창 19.3초 · 1,571MB · 「끔 (0)」 · 죽음 기록 0 · 자체점검 31/31
  한바퀴 1단 767자 · 찾음 12/20 · 인사 1,087자(me/learn 안내 +82, 상한 1,200) · 덮어쓰기보호 다 됨
  구운 서버 me/learn → 201 saved (시험으로 만든 「나에 대해」 는 치웠다)
```

- **「VC와 나」 갈래를 맨 앞에**(28문항): 부를 호칭 · 주요 업무 · 직책 · 맡기고 싶은 일 · 절대 하면 안 되는 일 · 혼자 정해도 되는 범위 · 꼭 물어볼 일 · 보고 방식 · 비밀 주제 · 스스로 배워도 되는 것 등. 일·기기 갈래에도 더 자세한 질문을 보탰다
- **「지침」 칸**: 여러 줄 자유 글 → 「나에 대해」 글 **맨 앞** `## VC에게 바라는 지침` (AI 가 펴면 지침부터 읽는다)
- **「외부 연결」 칸**: GitHub · Notion 토큰 → 운영체제 보관소(`connect:이름`)에만, 설정에는 이름만. 보관소 없는 OS 는 안 받는다(평문 안 둠). hello 가 `store.connections` 로 연결된 **이름만** 알린다. Google 은 OAuth 앱 등록이 필요해 **오너 결정 8**
- ★★ **쓰다 보면 저절로 채워진다**: `POST /eb/v1/me/learn`(category·question·answer·source) — AI 가 대화에서 알게 된 것을 「VC가 알아낸 것」에 **짐작**으로 적는다(출처·날짜). **오너가 적은 답은 절대 안 덮고(200 owner)**, 질문 창 빈 칸에 「VC 짐작: …」 으로 흐리게 보여 오너가 적으면 확인된 것으로 짐작을 치운다. hello 안내에 한 줄. 원격 PC 403
- 아직 없는 것: 창고 안 대화에서 VC **스스로** 짐작을 뽑는 분석 → **오너 결정 9** · Google 연결 → **오너 결정 8** · ~~연결한 계정에서 자료를 읽는 모듈~~ → v0.5.16 에서 냈다(`connect/…`)

### v0.5.14 묶음 (구웠다, 2026-09-13)

```
v0.5.14  VC-v0.5.14.zip (300.5MB)   SHA-256 EDE7B3904B2F3C0596D963987F136450A67A310B1B25CC492106E08E7D075426
[잰 것, 구운 v0.5.14 · 실무 창고 VC-실무\기록 2794장 · e5-base]
  --doctor v0.5.14 · 뜻 왕복 ✔ · Defender 이상 없음 · 창 20.1초 · 1,570MB · 떠 있는 창 1 · 「끔 (0)」 · 죽음 기록 0 · 자체점검 31/31
  한바퀴 1단 767자 · 찾음 12/20 · 인사 1,005자 · 덮어쓰기보호 다 됨
```


**단축키 창(F1)을 표로** — 공백으로 칸을 맞춰 비례 글꼴에서 줄이 들쭉날쭉했고, 한글 글꼴이 `\` 를 `₩` 로 그렸다. 표로 그리고 키는 강조색·모노, 보이는 `\` 만 전각 `＼`. 검사가 읽는 평문 `단축키글()` 은 그대로. 확인창(지우기·되돌리기)과 주 창 오른쪽 칸(제안 카드·활동·모델 고르개)은 그려 보니 이미 같은 결이라 그대로 뒀다

### v0.5.13 묶음 (구웠다, 2026-09-13)

```
v0.5.13  VC-v0.5.13.zip (300.5MB)   SHA-256 EE674A7F4E4D10B7997A8DE0328EDEF30C3CAB4D891F350A654B7B97160464F7
[잰 것, 구운 v0.5.13 · 실무 창고 VC-실무\기록 2794장 · e5-base]
  --doctor v0.5.13 · 뜻 왕복 ✔ · Defender 이상 없음 · 창 18.3초 · 1,569MB · 떠 있는 창 1 · 「끔 (0)」 · 죽음 기록 0 · 자체점검 31/31
  한바퀴 1단 767자 · 찾음 12/20 · 인사 1,005자 · 덮어쓰기보호 다 됨 · 진단 묶음 깨끗함
```

**질문(설정) 창을 주 창 디자인으로 다시 짰다(오너: 「너무 올드하다」)** — 윈도우 기본 탭·폼이라 이 창만 옛날 프로그램처럼 보였고, 탭 글자는 어두운 바탕에 어두운 붉은색이라 안 읽혔다. 이제 `theme.stylesheet()` 를 입히고 왼쪽 목록(`갈래  적은수/질문수`, 고른 줄은 붉은 막대) · 오른쪽 `// 소제목` + 한 줄 설명 · 모노 질문 글씨 · 밑줄만 있는 투명 칸 · HUD 테두리 · 오른쪽 아래 닫기/저장, 말할 게 있을 때만 뜨는 알림 띠. 빈 칸 안내 글은 팔레트로 흐리게. 자동 검사는 화면 없이 돌아 못 봤고, **창을 그림으로 그려(`grab()`) 눈으로 보고** 고쳤다. ※ 구운 VC.exe 는 시작 메뉴에 없어 화면 조작 도구 권한을 못 받는다 — 화면은 이렇게 그려서 본다. 본 창(글 열기·`⋯` 차림표에 설정·전체화면)도 그려 봤다 — 이상 없음

### v0.5.12 묶음 (구웠다, 2026-09-13)

```
v0.5.12  VC-v0.5.12.zip (300.5MB)   SHA-256 1687792B41B89579E88D17A639471AF8DF21A4964143E55B0A01D6AAAC55E344
[잰 것, 구운 v0.5.12 · 실무 창고 VC-실무\기록 2794장 · e5-base]
  --doctor v0.5.12 · 뜻 왕복 ✔ · Defender 이상 없음 · 창 16.9초 · 1,568MB · 떠 있는 창 1 · 「끔 (0)」 · 죽음 기록 0 · 자체점검 31/31
  한바퀴 1단 767자 · 찾음 12/20 · 인사 1,005자 · 덮어쓰기보호 다 됨 · 진단 묶음 깨끗함
  구운 서버로 모델을 골라도 설정 파일의 글자배율 칸이 남는다(잰 뒤 설정은 원래대로 되돌림)
```

**바깥 AI 제공자 고르는 칸(오너 결정 2 추천대로)** — 설정 창 「바깥 AI」: 자체 엔진 · Anthropic · Gemini · OpenAI 호환(주소). 키는 운영체제 보관소로, 칸을 비우면 쓰던 키 그대로, 친 키는 창에 안 남긴다. 다시 켜야 적용된다고 창에 말한다 · **클라우드로 켜면 그 제공자의 모델 이름을 쓴다**(전엔 깐 gguf 이름 `llama-8b` 가 Anthropic 에 나갔다) (키 평문·모델 덮기 되돌리면 둘 다 터짐. 검사는 가짜 보관소 — 진짜 키는 안 건드린다) ·
**모델을 고를 때 설정 파일을 통째로 덮지 않는다** — 서버가 켤 때 읽어 들고 있던 사본으로 `eb_config.json` 을 통째로 써서, 그 사이 화면이 적은 글자 크기·화면 방식이 **말없이 지워졌다**. 지금 파일을 다시 읽어 `models` 칸만 바꿔 쓴다

### v0.5.11 묶음 (구웠다, 2026-09-13)

```
v0.5.11  VC-v0.5.11.zip (300.5MB)   SHA-256 51371A6F04E0828BAE348CC4345F7B4D80D57B8D35225F41734E909B306E5F55
[잰 것, 구운 v0.5.11 · 실무 창고 VC-실무\기록 2794장 · e5-base]
  --doctor v0.5.11 · 뜻 왕복 ✔ · Defender 이상 없음 · 창 18.1초 · 1,571MB · 떠 있는 창 1 · 「끔 (0)」 · 죽음 기록 0 · 자체점검 31/31
  한바퀴 1단 767자 · 찾음 12/20 · 인사 1,005자 · 덮어쓰기보호 다 됨 · 쓰기 답 칸 title·mode·link_to·hint(path 없음)
  진단 묶음: 집 폴더 없음 · 글 파일 이름 0개 · 토큰 값 없음
```

**쓰기 답에서 절대 경로(`path`)를 뺐다** — 집 폴더(사용자 이름)가 들어 있고, AI 는 제목으로 부르므로 쓸 데가 없었다(쓸 때마다 글자만 탔다). protocol·잣대·화면 어디서도 안 썼다 (되돌리면 터짐)

### v0.5.10 묶음 (구웠다, 2026-09-13)

```
v0.5.10  VC-v0.5.10.zip (300.5MB)   SHA-256 3CC5429AF21C1CD4F24DEC85239FE705CC6D961C6A0A83DB43776D87E6817F1B
[잰 것, 구운 v0.5.10 · 실무 창고 VC-실무\기록 2794장 · e5-base]
  --doctor v0.5.10 · 뜻 왕복 ✔ · Defender 이상 없음 · 창 17.9초 · 1,570MB · 떠 있는 창 1 · 「끔 (0)」 · 죽음 기록 0 · 자체점검 31/31
  한바퀴 1단 767자 · 찾음 12/20 · 인사 1,005자 · 덮어쓰기보호 409·201·undo·치움 다 됨
  진단 묶음(--report, 실무 기록으로): 파일 4개 · 집 폴더 없음 · 글 파일 이름 0개 · 토큰 값 없음
```

★★ **진단 묶음 개인정보 구멍 둘(최우선 규칙)** — ① 설정.json 은 맨 위 칸의 토큰만 지워, `backend.api_key`(보관소 없는 OS·옮기기 전 평문)와 집 폴더가 든 모델 자리가 **그대로 담겼다** → 안쪽 칸까지 token·secret·key·password 를 지우고 집 폴더를 `~` 로 ② 기록 줄에 예외 문구로 딸려 온 **글 파일 이름**(`…\회의록.md`)이 담겼다 → 묶음 한 자리에서 `(글).md` 로. 오늘 넣은 기록 줄 셋에서도 글 이름·경로를 뺐다 (둘 다 되돌리면 터짐) ·
**같은 제안이 두 번 쌓이지 않게 「없을 때만 넣기」를 SQL 한 문장으로** — 화면(`ui.Store`)과 서버(`srv.Store`)가 한 프로그램 안에서 eb.db 를 따로 열어, 서버 쪽 잠금으로는 화면 「점검」과의 겹침을 못 막았다. `Store.add_proposal_if_new` 를 둘 다 쓴다 · 서버 분석 잠금은 걷었다(검사: 따로 연 두 저장소가 같은 제안을 넣으면 하나만 · 결정된 제안은 새 제안을 안 막는다)

### v0.5.9 묶음 (구웠다, 2026-09-13)

```
v0.5.9   VC-v0.5.9.zip (300.5MB)   SHA-256 14911717A83AF4B6C876108DCDC50F58A86ED07166475042A6571B5BAAB3DA78
[잰 것, 구운 v0.5.9 · 실무 창고 VC-실무\기록 2794장 · e5-base]
  --doctor v0.5.9 · 뜻 왕복 ✔ · Defender 이상 없음 · 창 18.4초 · 1,570MB · 떠 있는 창 1 · 「끔 (0)」 · 죽음 기록 0 · 자체점검 31/31
  한바퀴 1단 767자 · 찾음 12/20 · 인사 1,005자 · 덮어쓰기보호 409·201·undo·치움 다 됨
```

말로 시킨 쓰기 막힘은 **이미** `do_order` 장식이 까닭을 말하고 있었다 — 고칠 것은 없고 그 검사만 붙였다(까닭을 말하나 · 죽음 기록에 안 들어가나). ※ `ask()` 에 막이를 더 넣었다가 되돌려도 통과해서 걷었다 ·
**제안 카드 승인·고르기가 스킬 저장 막힘에 창을 죽이지 않는다** — 카드 단추 신호에서 `WriteBlocked` 가 새면 프로세스가 끝났다. 막히면 결정을 안 적어 제안이 남고(다시 누를 수 있다) 까닭을 말한다 (되돌리면 터짐) ·
**오늘 일지(Ctrl+D)가 쓰기 막힘에 창을 죽이지 않는다** — 신호 안에서 `WriteBlocked` 가 새면 PyQt 가 프로세스를 끝낸다. 읽기 전용 창고에서 Ctrl+D 한 번이 그 길이었다. 까닭을 말한다 (되돌리면 터짐) ·
**설정 창 저장이 막히면 창을 안 닫고 알린다** — 전엔 「저장」을 눌러도 창만 남고 왜인지 몰랐다(적은 것은 창에 그대로) (되돌리면 터짐)

### v0.5.8 묶음 (구웠다, 2026-09-13)

```
v0.5.8   VC-v0.5.8.zip (300.5MB)   SHA-256 63F8ACB3372C654B0538F61855EF7FBD7565B4639BE689E74F922F8EC1E00CED
[잰 것, 구운 v0.5.8 · 실무 창고 VC-실무\기록 2794장 · e5-base]
  --doctor v0.5.8 · 뜻 왕복 ✔ · Defender 이상 없음 · 창 17.5초 · 1,572MB · 떠 있는 창 1 · 「끔 (0)」 · 죽음 기록 0 · 자체점검 31/31
  한바퀴 1단 767자 · 찾음 12/20 · 인사 1,005자 · 덮어쓰기보호 409·201·undo·치움 다 됨
```

**덮어쓰기도 지금 글을 못 읽거나 판을 못 남기면 멈춘다** — 잠겨서 못 읽은 것을 「사라짐」으로 쳐 지난 판 없이 덮었다(옵시디언에서 고친 글이 흔적 없이 사라지는 길). 판 남기기의 날것 OSError 는 `WriteBlocked` 로(서버 500 → 507) · 맨 앞 옛 글 읽기(신원 챙기기)도 같다 (앞 읽기·판 남기기 되돌리면 터짐. 판 남기기 직전 두 번째 읽기 막이는 앞 읽기에 가려 이 검사로는 안 갈린다 — 그 사이 잠길 때만 쓰인다) ·
**지난 판을 못 남기면 지우기·되돌리기가 멈춘다** — 전엔 그냥 지우고(되돌릴 판 없이 사라짐) 그냥 덮었다(지금 글이 어디에도 없음). `WriteBlocked` → 서버 507 · 화면은 말로 시킨 지우기·되돌리기·무르기·지난 판 칸까지 알린다 · 이름 바꾸기가 지난 판 폴더를 못 옮기면 기록에 남긴다 (지우기 자리 되돌리면 터짐) ·
**말없이 삼키던 다섯 자리를 기록에 남긴다** — 쓰고 난 뒤 뜻 벡터 못 만듦(방금 쓴 글이 뜻으로 안 찾힘) · 인사의 창고 판 못 만듦(AI 헛검색) · 화면 미룬 조작 실패(누른 것이 말없이 안 먹음) · 뜻 모델 폭 못 잼(뜻 검색 꺼짐) · 찾기 전 낡은 벡터 못 채움 (인사 판 자리 되돌리면 터짐) ·
**창 색인 실이 실패하면 기록에 남긴다** — 훑기·뜻 모델 올리기·폭 재기·벡터 만들기 네 자리가 말없이 넘어갔다(구운 판은 콘솔이 없어 아무도 몰랐다). 서버 쪽 실 셋·엔진 실은 이미 남기고 있었다 (훑기 자리 되돌리면 터짐) ·
**화면에서 새 글 만들기가 잠그고 다시 본다** — 새 항목·빈 링크 채우기(말로 「만들기」도 이 길)가 「없다」고 본 뒤 쓰기 전에 AI 가 같은 제목을 쓰면 빈 글로 덮었다(겹침 검사는 없다 — Qt 메서드라 딴 실에서 못 부른다. 도는 길은 ui_check 가 본다)

### v0.5.7 묶음 (구웠다, 2026-09-13)

```
v0.5.7   VC-v0.5.7.zip (300.5MB)   SHA-256 ECA47B080C80589AF4FBD6C11219639EAE2704E3196A289AC2FD0318B473A638
[잰 것, 구운 v0.5.7 · 실무 창고 VC-실무\기록 2794장 · e5-base]
  --doctor v0.5.7 · 뜻 왕복 ✔ · Defender 이상 없음 · 창 18초 · 1,570MB · 떠 있는 창 1 · 「끔 (0)」 · 죽음 기록 0 · 자체점검 31/31
  한바퀴 1단 767자 · 찾음 12/20 · 인사 1,005자 · 덮어쓰기보호 409·201·undo·치움 다 됨 · 틀린 로그(outcome "성공") 400
```

**분석끼리 줄 선다** — 「같은 제안 있나」 보고 넣기가 창·폰·주기 분석에서 겹치면 같은 제안이 두 번 쌓였다(되돌리면 「안 기다린다」) ·
**학습 로그 문이 정해진 이름·꼴 밖의 값에 400** — `Literal` 은 막지 않아 `outcome: "성공"` · `tier: "gpu"` · 숫자 칸 글자가 그대로 저장되고 분석에서 말없이 빠졌다. `LogEvent` 가 검증(되돌리면 터짐 · 창고 쪽 `_emit` 은 이미 맞는 값만 보낸다) ·
**Content-Length 가 음수·글자면 바로 400 으로 끊는다** — 둘 다 500 에 까닭 없음이었다 [잰 것, 되돌려서]. 「음수면 실을 붙든다」는 짐작이었고 재 보니 500 이었다 (둘 다 되돌리면 터짐) ·
**남은 문 둘도 글자가 아닌 값에 400** — `remote/close` session · `chat/completions` model (전엔 200·백엔드로 샘, 되돌리면 터짐). `models/download` key · `models` role·name 은 원래 400 이라 막이를 안 넣고 검사만 붙였다(넣었다가 되돌려도 통과해서 걷음) ·
**흡수가 쓰기 직전에 제목을 다시 본다** — 제목 목록은 흡수 시작 때 뜬 것이라, 그 사이 AI 가 같은 제목을 쓰면 조용히 덮었다. 잠그고 다시 보고 생겼으면 다른 제목

### v0.5.6 묶음 (구웠다, 2026-09-13)

```
v0.5.6   VC-v0.5.6.zip (300.5MB)   SHA-256 1F840EDCDF7F687FA88163E3E872E3BC93F3F6B6F09B81CAAEE65A47B991AA15
[잰 것, 구운 v0.5.6 · 실무 창고 VC-실무\기록 2794장 · e5-base]
  --doctor v0.5.6 · 뜻 왕복 ✔ · Defender 이상 없음 · 창 18초 · 1,571MB · 떠 있는 창 1 · 「끔 (0)」 · 죽음 기록 0 · 자체점검 31/31
  한바퀴 1단 767자 · 찾음 12/20 · 인사 1,005자(정규식 안내 +18) · 덮어쓰기보호 409·201·undo·치움 다 됨
  정규식 검색 /결정 \d+/ → 5장
```

**부하 때 덧붙이던 실이 죽던 것(열린 문제 17)** — 한 글 색인·`forget`·지우기의 여러 줄을 연결 잠금으로 통째로 쥔다. `forget` 은 「같은 제목 남았나」 보고 지우는 틈에 옮긴 글의 링크를 지울 수 있었다 (검사: 안의 execute 마다 잠금을 쥐었나 — 되돌리면 False · 부하 넷 동시 두 번 통과) ·
**`/정규식/` 검색(옵시디언 대응)** — 낱말 색인이 기호에서 끊겨 못 찾던 `ERR-1234` 같은 꼴을 찾는다. 다른 말과 같이 쓰면 그 말로 넉넉히 찾고 거른다 · 틀린 정규식 0장 · 창·AI 같은 길(`notes.search`) · hello 안내·protocol 에 적음 (되돌리면 터짐)

### v0.5.5 묶음 (구웠다, 2026-09-13)

```
v0.5.5   VC-v0.5.5.zip (300.5MB)   SHA-256 789CE6C378F13CEDE2D62234CE5B2BF7E8423A3DC5D4C32B3DA700E5B21767C2
[잰 것, 구운 v0.5.5 · 실무 창고 VC-실무\기록 2794장 · e5-base]
  --doctor v0.5.5 · Defender 이상 없음 · 창 19초 · 1,570MB · 떠 있는 창 1 · 「끔 (0)」 · 죽음 기록 0 · 자체점검 31/31
  한바퀴 1단 779자 · 찾음 12/20 · 인사 987자 · 덮어쓰기보호 409·201·undo·치움 다 됨
  ★ 찾음 12 · 뜻 왕복 ✘ 는 구운 v0.5.4 도 같은 벡터에서 똑같다(열린 문제 19) — 이 판 탓 아님
```

[잰 것, 소스 · 실무 창고 · e5-base] 한바퀴 1단 779자 · 찾음 12/20 · 전체 33,104자 · 인사 1,032자 · 덮어쓰기보호 다 됨.
★ 구운 v0.5.4 는 13/20 · 인사 987 인데 **v0.5.4 소스도 같은 창고에서 12/20 · 1,032** — 고침 탓이 아니라 소스와 구운 판 차이다(인사는 소스 쪽 대화 모델 목록이 길다). 견줄 때는 **구운 판끼리** 견준다.

**이름 바꾸기가 새 이름을 잠근다** — 「없다」고 본 뒤 그 이름이 생기면(두 이름 바꾸기가 같은 새 이름으로 · AI 가 그 제목으로 새 글) 한쪽 글을 덮었다. 이름 바꾸기끼리 줄 세움(되돌리면 「안 기다린다」) ·
**바깥 스킬 문(오너 결정 3 추천대로)**: `POST /eb/v1/skills/propose` — 선언문만 · 제안 줄로 · 사람 승인 전엔 스킬 아님 · 있는 모듈만(모르면 400 에 `known`) · 원격 403 (되돌리면 `../../etc/passwd` 가 202) ·
승인이 선언문의 모르는 칸에 500 나던 것 막음(되돌리면 터짐) ·
원격 PC 의 `skills/propose` 는 403(원격 문이 `/eb/v1/skills` 를 앞머리로 열어 둬 닿는다 — 되돌리면 넣어짐) ·
**바깥 AI API 키를 운영체제 보관소로(오너 결정 1 추천대로)**: `keystore.py` — 윈도우 자격 증명 관리자(ctypes) · 맥 `security`, 새 의존성 없음. `load_config` 가 평문 `api_key` 를 옮기고 설정에서 지운다(못 옮기면 그대로) · `backends.build` 가 보관소에서 꺼낸다 (둘 다 되돌리면 터짐. 검사는 시험 종류 이름을 써서 진짜 키를 안 덮는다) ·
**`--판올리기` 도 글마다 잠근다** — 창고 전부를 읽고-다시-쓰는 길이라 켜 둔 VC 에 AI 가 쌓던 줄을 덮을 수 있었다(잠금만 둘렀고 겹침 검사는 없다 — 도는 길은 `--판올리기` 검사가 본다) ·
**지우기도 글 잠금 안에서** — 지난 판 남기기와 지우기 사이에 붙인 줄이 이력에도 없이 사라졌다(되돌리면 「안 기다린다」) ·
**`memory/rename` 이 기다리다 물러나면 409**(전엔 「없는 글」 404 — AI 가 멀쩡한 옛 글을 헤맨다) ·
**제안 카드가 부르는 모듈을 직접 보인다** — 요약은 제안한 쪽 글이라 거짓일 수 있다. 「바깥에서 온 스킬 · 부르는 것: navigate(to=집)」(되돌리면 터짐)

### v0.5.4 묶음 (구웠다, 2026-09-13)

커밋됨: 알림 기록 남김(b6bad8c) · 흡수 「못 읽음」 셈(aa253d5) · 오너 결정 모음(7a5c42a) · 창 쓰기 막힘 알림(d781453) ·
장식 checked 인자(f2e7c5a) · **같은 글 동시 덧붙이기 줄 손실**(982cb01, 두 실 40/80·두 프로그램 41/80 → 80/80) ·
**새 글 동시 첫 덧붙이기 손실**(2d79731·되돌리면 36 중 15) · **413 뒤 RST**(25f4c4c, 60번 중 4번 → 0)

**이름 바꾸기 링크 고침도 글 잠금 안에서**(되돌리면 300 중 113) ·
**sqlite 연결 하나를 서버 실들이 겹쳐 쓰던 것**(`paths.잠근연결`, `Notes`·`Store` 둘 다 — 되돌리면 `IntegrityError`) ·
**읽고-고치고-쓰기 넷을 글 잠금 안으로**: 서버 덮어쓰기 막이(되돌리면 읽은 뒤 생긴 글을 force 없이 덮음 201) ·
화면 고정·풀기 · 스킬 저장·되돌리기 · 화면 「무르고」(옛 몸으로 되돌려 그 사이 AI 줄까지 지우던 것 → 붙인 글만 뺌) ·
글 잠금 겹쳐 잡기(같은 실이면 지나감, 되돌리면 멈춤) ·
**설정 창 새로(오너 요청)**: `settings.py` — 화면 방식 창·최대화·전체화면(켤 때 적용, F11 켜고 끔, `⋯`·Ctrl+,) ·
「내 정보」 13갈래 질문 100개 + 내 질문 더하기 → 창고 글 `나에 대해`(갈래 preference, **고정 안 함** — 고정이면 검색마다 맨 앞에 실려 글자를 태운다) ·
밖에서 적은 칸·줄은 안 지움(되돌리면 지움) · hello 가 `store.me` 로 제목을 알림 ·
**읽고-쓰기 넷 더 잠금 안으로**: 오늘 일지 만들기 · 지난 판 되돌리기(둘 다 되돌리면 「안 기다린다」) · 화면 저장 · 할 일 뒤집기

v0.1.94 지시서 19 **끝났다** (Codex, 2026-09-12) — **급함 닫혔다.** 네 세 물음이 다 1등으로 나왔다
(「야간 항해를 돕는 해안 표지」·「안개 속 등대가 선박을 인도한다」·「lighthouse」).
네가 「못 쟀다」고 적은 `[뜻 벡터]` 콘솔 줄은 **네 방법이 틀린 게 아니었다** — 구운 판은
`--noconsole` 이라 `sys.stdout` 이 `None` 이고 `print` 가 조용히 사라진다. v0.1.95 에서 파일로 남긴다.

v0.1.93 지시서 18 **끝났다** (Codex, 2026-09-12) — ①찾기점수 자동 채움 ✔ · ③--doctor 판 줄 ✔ ·
②--no-ui 는 **반쪽이었다**(벡터는 자랐는데 뜻 검색 0건). 그 급함이 v0.1.94 의 고침이다.

v0.1.88 지시서 10 · v0.1.89 지시서 11 · v0.1.90 지시서 12 **다 끝났다** (Codex, 2026-09-12) — 전부 통과.
지나간 판의 zip·지시서·보고서는 만든 쪽이 보관하고 공유폴더에서 치웠다.
★★ **고리를 잠시 멈춘다 (오너 지시 2026-09-12).** 앞으로 검증은 **만든 PC 에서** 한다.
`테스트시작.txt` 는 **안 올라온다** — 기다리지 마라. 다시 켤 때 이 줄이 없어지고 그 파일이 생긴다.
그동안 고마웠다. 네가 잡아 준 급함 하나(`--no-ui` 뜻 검색 0건)가 v0.1.94 를 만들었고,
네가 낸 설계 하나(`--doctor` 뜻 왕복)가 v0.1.95 가 됐다. 둘 다 그대로 들어갔다.

## 열린 문제

| # | 무엇 | 상태 |
|---|---|---|
| 2 | 문지기 판정이 한 기계 안에서 갈렸다 | **소스로 돌린 첫 판만** 갈렸다. 구운 판끼리·네 자리는 되풀이된다. 네 자리에서 다시 잴 필요 없다 |
| 4 | 창이 서버에 못 물어보는 것 | v0.1.87·88 에서 재현 안 됨. 지켜만 본다 — 나면 시험표 「화면」 줄의 「서버 전화 마지막 탈」을 옮겨 적어라 |
| 20 | 스스로 짐작(selflearn)의 정확도 — 진짜 1.5B 로 5개 중 3개만 맞다 | **지켜봄.** 틀린 짐작은 질문 창에서 흐린 「VC 짐작」 으로만 보여 오너가 안 적으면 확정되지 않는다. 바깥 AI(클라우드)로 켰거나 더 큰 로컬 모델을 깔면 다시 잰다(`시험기록` 에 쓴 말 넷·짝 다섯 그대로) |
| 19 | **벡터를 누가 만들었나에 따라 찾음이 13 ↔ 12** (실무 창고 · e5-base 같은 모델) | **열림.** 구운 판이 만든 벡터에서 구운 v0.5.4 13/20 · `--doctor` 뜻 왕복 미확인 → 소스 서버가 768 로 다시 만든 뒤 구운 v0.5.4·v0.5.5 둘 다 12/20 · 뜻 왕복 ✘(「(A) 사람이 손으로 — 화면 쪽」 → 「같은 창에서 VC 를 켠다」 0.876). ~~짐작: 소스와 구운 판의 임베더 차이~~ **틀렸다** — onnxruntime 1.27.0 · numpy 2.4.6 같고, e5-base `model.onnx`·`tokenizer.json` 지문도 같다. [잰 것] 구운 v0.5.5 로 `--색인다시` 뒤 벡터 2794장을 통째로 다시 만들었더니(220초) **찾음 12/20 그대로 · 1단 767자 · `--doctor` 뜻 왕복 ✔**. → 지금 창고·지금 판의 값은 **12** 다. 13 은 그날 오간 벡터 위의 값으로 본다 [짐작이다]. 뜻 왕복 ✘ 는 소스가 이어 만든 벡터에서만 났다 — 다시 나면 그때 판다 |
| 18 | 창 메모리 v0.5.3 1,185MB → v0.5.4 1,565MB | **닫힘 — 코드 탓 아님, 뜻 모델 차이.** 구운 v0.5.4 실무 창고: 큰 모델(e5-base) 1,565 · 뺀 채 1,357. 소스 v0.5.3·v0.5.4 같은 모델 자리 1,561·1,568, 모델 폴더 없으면 1,178. VC-실무 에 e5-base 가 들어간 것이 14:06 — v0.5.3 을 잰 것은 그 무렵이다. ★ 메모리는 **모델 자리를 같게 두고** 견준다 |
| 17 | 이름 바꾸기+덧붙이기 고리 검사가 **부하 걸린 때** 「300 중 18·26·291」 (2026-09-13) | **원인 잡음 → v0.5.6 묶음에서 고침.** 줄이 사라진 게 아니라 덧붙이던 실이 `_index_file` 에서 `sqlite3.IntegrityError: constraint failed` 로 **죽었다**(검사 셋 동시에 띄워 stderr 로 잡음). 연결 잠금이 한 줄씩만 줄 세워, 한 글 색인의 여러 줄 사이에 다시 훑기가 끼었다. 한 글 색인을 연결 잠금으로 통째로 쥔다. ~~지켜봄.~~ 그 사본은 되돌리기 잠금만 뺀 것이라 이 고리와 무관. 진짜 코드 혼자 3번 · CPU 가득 부하로 4번 모두 300/300. 다시 나면 그때 동시에 돌던 것을 같이 적는다 |
| 16 | `--모두검사` 에서 `ui` 가 한 번 임시 폴더 정리 중 `PermissionError [WinError 32]` (2026-09-13) | **지켜봄.** 혼자 5번 0 · 모두검사 다시 30/30. 다시 나면 traceback 전체를 남긴다 |
| 12 | 네 long 창고 죽음 기록의 `access violation` 1줄 (2026-09-12 02:19:54) | **지켜봄.** 원문(지시서 13)으로 갈랐다 — 같은 1초에 v0.1.88 이 딴 작업 폴더에서 못 뜨며 traceback 2개를 남겼고(**v0.1.89 에서 고친 그 결함**, 닫힘), 접근 위반 줄 밑 스레드 목록은 **서버가 돌고 뜻 모델을 올리던 다른 VC 창**의 것으로 보인다 [짐작이다]. faulthandler 는 처리될 예외도 먼저 적으므로 그 창이 **실제로 죽었는지는 모른다.** ★ 다시 나면 그 판 `vc-기록.log` 에 그 창의 「끔 (0)」 이 찍혔는지 같이 적어라. [잰 것, 30분 실사용] **그 30분 동안은 죽음 기록이 한 줄도 안 자랐고**(COM 1줄 그대로) 창은 「끔 (0)」 으로 끝났다. [잰 것, v0.1.91] 새 창고에서도 COM 1줄 · 그 밖 0 · traceback 0 — **새 죽음 종류는 안 나왔다** |

## 닫힌 문제 (확인됨)

- **자체점검은 `--모두검사` 한 줄이다 (v0.2.5).** `pc/` 의 **스물아홉 개**가 다 돈다(74초).
  그전에는 한 번에 도는 길이 없어 사람이 외워야 했고, `panels`·`paths` 를 한동안 안
  돌리고 있었다. 새 모듈은 저절로 든다. **판을 내기 전에 이것만 돌리면 된다.**

- **죽은 검사를 다섯 되살렸다 (2026-09-12~13).** `assert "…" in 본문` 꼴은 찾는 글자가
  **검사문 안에 있어** 진짜 코드를 지워도 통과한다. `--doctor` 셋 · 찾기점수 둘이 그랬다.
  개수로 세거나(코드 1 + 검사문 1 = 둘이 바닥) **실제로 불러 본다.**
  ★ `notes.py` 에 같은 것이 **두 벌** 있던 것(164줄)도 이때 드러났다 — 되돌려 터뜨려 봤는데
  아무 일도 안 일어나서 알았다. 파이썬은 뒤엣것이 이기는데 **고칠 때는 앞엣것이 먼저 보인다.**

- **15 · 화면에서 뜻 모델을 바꾸는 길 — 냈다 (v0.2.2).** `ROLES` 에 `meaning` 이 없어서
  고르는 칸이 **아예 안 만들어지고 있었다.** 받아 두고도 되돌릴 길이 없었고, 받으면
  좋다는 것도 몰랐다. 이제 「뜻 검색」 칸에 「딸려 온 것 (e5-small)」·「e5-base」 가 뜨고,
  고른 것을 `meaning_dir(고른것)` 이 먼저 본다. 없는 것을 고르면 있는 것으로 내려간다.
  ★ 이 문제에 값이 생긴 것은 오늘이다 — [잰 것] 큰 것이면 찾은 물음 **10 → 12**.

- ★★ **AI 가 싸게 꺼내 쓰게 하는 것이 이제 첫 잣대다 (오너 지시 2026-09-12).**
  「찾았나」만 재던 것에 **「몇 글자를 태웠나」**를 더했다(`시험기록\잣대-꺼내기값.py`).
  오너 창고 2794장·얼린 물음 20개로 잰다. 하루 동안:

  | | 처음 | 지금 |
  |---|---:|---:|
  | 1단 search | 1716자 | **793자** |
  | 한 물음(1단+2단) | 2273자 | **1306자** |
  | `/eb/v1/graph` | 98,425자 | **13자** |
  | 찾은 물음 | 6/20 | **10/20** (갈래까지 좁히면 13/20) |

  고친 것: 빈 칸·기본값 안 보내기 · 요약을 물음에 맞춰 고르기 · `k` 8→5 ·
  **갈래를 돌아가며 뽑기**(한 갈래가 목록을 다 차지했다) · 좁힘을 세기 **전에** 걸기
  (세고 나서 좁혀서 전멸했다) · 404 에 가까운 제목 · 조사를 넘는 낱말 찾기.
  재서 **진** 것도 적어 뒀다: 점수 싣기 · 낱말 검색 살리기(둘 다 손해).

- **v0.1.94 · --no-ui 뜻 검색 — 네 자리 확인.** v0.1.93 에서 0건이던 세 물음이 다 1등.
  「자라는 것과 쓰이는 것은 다른 말이다」가 이 판의 교훈이고, v0.1.95 의 「뜻 왕복」이
  그걸 **프로그램이 스스로 묻게** 만든 것이다 — 설계는 네가 냈다.

- ★★ **자라는 것과 쓰이는 것은 다른 말이다 (2026-09-12, 네가 잡음).** v0.1.93 에서 창 없는
  서버도 뜻 벡터를 만들게 했는데, 그 실이 **제 sqlite 연결에만** 임베더를 붙였다.
  `/eb/v1/memory/search` 가 쓰는 것은 서버가 들고 있는 **다른 `Notes`** 라 `_embed` 가
  `None` 이었고, `semantic()` 이 늘 빈 목록을 줬다 — 낱말은 걸리는데 뜻으로 물으면 0건.
  네 짐작(「뜻 벡터 경로가 적용되지 않는 것으로 보인다」)이 정확했다.
  ★ 내가 「벡터가 자란다」까지만 재고 「그 벡터가 쓰인다」를 안 쟀다. v0.1.94 에서 고쳤고,
  자체점검이 두 자리(임베더 붙이기 · 캐시 비우기)를 각각 되돌리면 터진다.

- **제목 벡터 — 기본값 안 바꾼다 (2026-09-12).** 네 창고(항목 5개)에서는 켜든 끄든 5/5 로
  **안 갈렸다**. 네가 「작아서 포화됐다·일반화 못 한다」고 적어 준 그대로다.
  `--제목벡터빼고` 스위치는 재는 길로 그대로 둔다.
  ★ **낮에 「내 창고에서는 빼면 1등 2→5」라고 적었는데 그 값이 틀렸다** — 아래 「낡은 벡터」 자국을
  보라. 깨끗한 창고에서 다시 재니 **1등은 2 그대로, 3등 안만 4→5** 다. 얻는 것은 한 칸뿐이라
  **지시서 17 로 너에게 시킨 비교는 애초에 값이 작은 것이었다.** 헛걸음시켜 미안하다.

- ★★ **낡은 벡터가 잣대를 속였다 (2026-09-12 저녁).** 내 쪽 잣대 사본에서 **낡은 벡터가 정확히
  14개**였고, 그 14개가 **잣대 정답 글 14장과 완전히 같았다**(넘치지도 모자라지도 않았다).
  글은 그날 14:45:52 에 만져졌는데 벡터는 일주일 전 것이었다 — 곁실험 하나가 정답 글에
  「무엇에 대한 글인지」 한 줄을 붙여 놓고 **벡터를 다시 안 만든 채** 재고서 「글을 고쳐도
  안 찾는다」고 적었다. 벡터만 채우니 같은 창고가 **1등 2→10 · 목록 밖 14→4**.
  → **v0.1.93 에서 `--찾기점수` 가 재기 전에 못 만든 벡터를 스스로 채운다.** 전에는 세기만 했다.
  ★ 너도 같은 함정에 걸린다: **글을 고쳤으면 벡터부터 채우고 재라.** 머리글의 「아직 못 만든
  벡터 N개」가 0 이 아니면 그 값은 못 믿는다.

- **토막 벡터는 안 넣는다 (2026-09-12).** 글을 소제목마다 쪼개 토막마다 벡터를 만들어 봤다
  (2794장 → 5723장). 깨끗한 창고에서 1등 2→3 · 목록 밖 13→13 으로 **한 칸만 움직였고**,
  한 줄 붙은 창고에서는 1등을 되레 깎았다(10→9). 스키마를 고칠 값이 없다

- **v0.1.92 · 재는 자리를 숫자가 밝힌다 — 네 자리 확인.** 빈 창고에서 「물음 5개 · 1등 0 … · **항목 0개**」가 찍혀
  「못 찾는 것」과 「잴 자리가 빈 것」이 갈렸다. 네 long 창고에서는 5문항 다 1등 · 못 만든 벡터 0.
  모델 크기 방어도 걸려 `matmul` 로 안 터졌다

- **13 · 말투(값 뒤 조사) — v0.1.91, 네 자리 확인.** 여섯 문장이 다 받침을 따라갔다:
  「회의록을」/「연결 메모를」 · 「"왕복"은」/「"사과"는」 · 「'tag:마침'은」/「'tag:사과'는」 · 「'회의록'은 이미 있어」.
  ※ 네 말대로 **이 몇 가지로 모든 끝말을 보증하지는 않는다** — 어긋난 것을 보면 그때 적어라(검사가 값 뒤 조사를 막는다)
- **14 · 접근성 이름 — v0.1.91, 네 자리 확인.** 트리에 「편집 검색칸」·「편집 제목칸」으로 갈려 보이고, `Ctrl+O` 없이 이름만으로 골라 검색·제목 수정이 됐다.
  ※ 도구의 `focused_element` 는 제목칸을 고른 뒤에도 검색칸으로 남았다 — **도구 쪽 값이라 VC 결함으로 안 센다**

- **5 · 30분 실사용 — 2026-09-12, 네가 30분 24초.** 새 글·제목 바꾸기·태그·`[[링크]]` 왕복·목차·따옴표/`tag:` 검색·곁에 띄우기·밖에서 고치기 모두 됐고, **1초 넘게 기다린 조작이 없었다.** 나온 것은 말투 셋뿐(→ 13). 지우기·휴지통 되살리기는 무인 판이라 안 했다

- **8 · 죽음 기록 가르기 — v0.1.90, 네 자리 확인.** 진단 묶음 11·1·2 = 네가 직접 센 11·1·2. 죽음은 「그 밖의 네이티브 예외」·「traceback」 쪽만 센다
- **9 · 없는 말 검색 문구 — v0.1.90, 네 자리 확인.** 「'어디에도없는말'이 든 글은 없어. 뜻으로 가까운 것 5개야.」 · 있는 말은 「'검증' 관련 5개야.」

- **10 · 딴 작업 폴더에서 켜면 안 뜨던 것 — v0.1.89 에서 고침, 네 자리 확인** (System32 작업 폴더 · 창 관찰 도구로 직접 띄우기 둘 다 뜸, System32\data 안 생김)
- **11 · 구운 판이 목소리 모델을 못 찾던 것 — v0.1.89 에서 고침, 네 자리 확인** (`--doctor` 「목소리 모델 있나」 true)

- **같은 폴더에 덮어 풀고 처음 켤 때 방화벽 창 — 안 뜬다** (v0.1.88, 방화벽 이벤트 0건). 판마다 오너가 누를 일이 없다
- 7 · 편집 중 검색칸에 친 말이 글로 들어간 것 — VC 결함이 아니었다. 만든 PC 에서 Claude 앱 창이 검색칸 자리를 덮어 누름이 VC 에 안 닿았다
  ★ 화면 조작 도구로 누름이 안 먹으면 그 자리 맨 위 창이 누구인지부터 본다. 검색칸은 `Ctrl+O` 로 가면 확실하다
- 6 · 시험표 「2단 한 토막」 — v0.1.88 에서 고침, 네 자리 831 「입력과 경계 조건」 확인
- 3 · 창·서버가 exe 옆 모델을 못 찾던 것
- 목차 이동 · `[[링크]]` 눌러 가기 · ← · Alt+← — v0.1.87·88 양쪽 통과
- 밖에서 고친 줄을 창·API 가 받는다 · 휴지통 글은 검색에 안 나온다

## 안 바뀔 값 (바뀌면 보고) — **무엇으로 쟀는지 같이 적는다**

```
「긴 글 검증 절차」 원본 3069자 · 전체 JSON 글자 수 · Bearer 로 서버에 직접
  note 한 편                              3226
  note 한 토막 「입력과 경계 조건」          831      (시험표 「2단 한 토막」)
  note 한 토막 「저장과 중복 방지」          849
  search 1단 · search full=1             356 · 3457  (장문만 있는 창고 기준)
※ 네 long 창고는 VC.md 의 「[[긴 글 검증 절차]]」 줄 때문에 542 · 3713 이 나온다 — 그 창고에서는 이 값이 맞다
※ 없는 소제목 404 · search 글자 수는 **물은 말 길이를 따라 바뀐다** · full=1 은 search 쪽 인자다

fixtures 6장 · 빈 창고 · --쓴다   흡수 남긴 3 · 버린 5 → 다시 세기 후보 0 · 겹침 9 · 휴지통 5
                                AI 1단 283 · 한 편 625 · full=1 854 (양쪽 같다)
--check 통과 · 기록자리.txt 를 exe 옆에서 읽음 · 씨앗이 있어도 첫 흡수가 막히지 않음
--사본치우기 · --판올리기 가 말을 한다(stdout) · 떠 있는 최상위 창은 주 창 하나
같은 폴더에 덮어 풀면 방화벽 창이 안 뜬다
```

## 닫힌 것 — 다시 묻지 않는다

- 카드 위 줄 정렬: 지금 그대로 (오너 결정)
- **문지기 모델: 지금 것(qwen2.5-1.5B) 그대로 — 기계 사이 판정 차이는 쫓지 않는다** (오너 결정 2026-09-12).
  잘못 버려도 휴지통에 남아 되살릴 수 있고, 모델을 키워도 기계마다 갈리는 것은 남는다.
  문지기는 **한 기계 안에서 되풀이되나 · 잡담·로그가 버려지는 방향인가**로만 본다.
  기계 사이 「어느 글이 남았나」 목록은 견주지 않는다. 실사용에서 남아야 할 글이 **되풀이해** 버려지면 그때 다시 본다.
