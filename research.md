# 이비 관련 연구 정리 (2026-08 기준)

이 문서는 **읽을 논문 목록이 아니라 결정과 연결된 근거표**다. 각 항목은
"이비의 어느 결정을 뒷받침하거나 반박하는가"로 정리한다. 뒷받침이 안 되는 논문은
아무리 유명해도 안 적는다.

먼저 확인한 것: 앞서 정리된 목록의 논문들은 **실재한다**(2607.08010 · 2604.27891 ·
2504.10519 직접 확인). 없는 논문 위에 설계를 얹으면 안 되므로 확인부터 했다.

---

## 1. 이미 우리가 하고 있는 것 — 근거가 붙은 결정들

### 스킬을 컴파일해 두고 런타임엔 호출만 (결정 17·30)

**Tool-Making and Self-Evolving LLM Agents in Low-Latency Systems** (arXiv 2607.08010,
2026-07, Kujanpää 외, Amazon)

반복되는 절차를 **배포 전에** 검증된 툴로 만들어 두고, 런타임에는 코드 생성 없이
호출만 한다. 44노드 SOP를 다루는 실제 알람 분류 시스템, 1,500건 이력 기준:

- 중앙값 지연 **42% 감소**
- 오류율 최대 **53% 감소**
- 툴이 구조화된 출력을 주면서 구조를 더 단순화 → 추가 **62% 지연 감소**

핵심은 "환경 그라운딩" — 실행 트레이스와 **실제 스키마를 관찰한 뒤** 코드를 만든다.
상상해서 만들지 않는다.

> **이비**: `skills.py`의 선언적 스킬이 정확히 이 모양이다. 다만 우리는 아직 코드를
> 만들지 않고 **선언(시작 단계·트리거·예시)만** 바꾼다. 그게 결정 17의 안전선이고,
> 이 논문의 "배포 전 검증"을 더 보수적으로 지킨 형태다.
> **보강할 것**: 이 논문의 "실행 트레이스 관찰 → 후보 생성 → 라벨된 케이스로 검증"
> 3단계 중 우리는 1·2단계만 있다. **검증 단계가 없다.**

### 승인 게이트와 되돌리기 (결정 18) — 가장 강한 뒷받침

**When Self-Evolution Backfires: Pre-Commit Gating against Skill Contamination in
LLM Agents** (arXiv 2608.05810, 2026-08-06, Shang 외)

이 논문 하나가 결정 18을 정면으로 정당화한다.

- **스킬 오염(skill contamination)**: 결함 있는 스킬이 스킬 풀에 들어가면 이후 학습되는
  스킬의 참고 자료가 되어 **오염 사슬**을 만든다. 되돌리기가 구조적으로 불가능해진다.
- **능력-오염 상전이**: 스킬이 쌓일수록 성능이 오르다가 **임계 크기를 넘으면 무너진다.**
- 무조건 축적한 뒤 나중에 나쁜 스킬을 지워도 **성능이 조금밖에 안 돌아온다**
  (rollback recovery gap).
- 대책 VaG(Verifier-as-Gatekeeper): **구조 타당성 · 행동 무해성 · 의미 일관성** 세 심사관.
  서로 대체 불가능하고 각자 다른 종류의 나쁜 스킬을 잡는다.
- 결과: pass@1 72%, **스킬 풀 크기 약 1/5**, 매 라운드 단조 개선.

> **이비**: "자동 생성 스킬을 자동 적용하지 않는다"는 결정이 취향이 아니라 **측정된
> 실패 모드에 대한 대책**임이 확인됐다. 우리 승인 게이트 = 사람이 심사관 역할.
> **보강할 것**: 우리는 심사가 사람 하나뿐이다. 이 논문의 세 심사관 중 **구조 타당성**
> (선언이 문법적으로 맞고 없는 모듈을 안 가리키는가)은 기계가 할 수 있다.
> 이미 `orchestrator._compose`에서 없는 모듈을 막고 있으니 절반은 있는 셈.
> **스킬 풀 크기 상한**은 없다 — 상전이가 실재한다면 필요하다.

### 온디바이스 우선, 필요할 때만 클라우드 (결정 5·24·25)

**Toward Super Agent System with Hybrid AI Routers** (arXiv 2504.10519, Yao 외, TensorOpera)

라우터가 작업 복잡도를 보고 로컬/클라우드를 고른다. "대부분의 연산은 로컬에서 처리하고
클라우드는 필요할 때만." 클라우드는 비용·지연·프라이버시를 모두 문제 삼는다.

**Small Language Models are the Future of Agentic AI** (arXiv 2506.02153, Belcak 외, NVIDIA)

에이전트 루프 안의 작업 공간은 **좁고 반복적**이라 특화된 작은 모델이 범용 거대 모델을
자주 이긴다. 실무 보고서들은 에이전트 작업의 **80~90%가 로컬에 남는다**고 본다.
규칙: **SLM 우선, 확신 낮거나 스키마 위반일 때만 승격.**

> **이비**: `brain.py`가 정확히 이 규칙이다(문턱 미달·격차 부족 시 승격/되묻기).
> 그리고 우리 실측이 이걸 확인해줬다 — 라우팅 판정 **5ms**, 실행 0.001s.

### 개인 사전(prior)으로 라우팅 보정 (결정 22·31)

**Statistical Priors for Implicit Preferences** (arXiv 2606.05828)
**Beyond Similarity: Trustworthy Memory Search for Personal AI Agents** (arXiv 2606.06054)
**PGMem: Tightly Coupled Persona-Memory Graph for Lifelong Personalized Agents**
(arXiv 2608.01708)

PGMem이 특히 우리 구조와 닮았다: 사건 노드와 페르소나 노드를 **출처·근거 간선**으로
잇고, 각 개인 신호가 **그것을 뒷받침한 사건까지 추적 가능**하게 한다. 기존 시스템이
페르소나를 사건과 분리된 납작한 프로필로 저장하는 걸 문제로 짚는다.

> **이비**: 우리 항목 그래프(`notes.py`)와 활동 흐름(제안의 근거를 같은 화면에 둔 것)이
> 같은 생각이다. 화면에서 "제안 ← 근거 기록"을 붙여 둔 게 이 논문의 provenance edge다.
> **보강할 것**: 우리는 제안에 `based_on`(지시 id)만 붙인다. **항목 자체에는 출처가
> 없다.** "카페 단골"이 어느 지시에서 왔는지 못 되짚는다.

---

## 2. 아직 안 하고 있는 것 — 모듈이 늘면 반드시 온다

### 라우팅 붕괴 구간

**Scaling Enterprise Agent Routing** (arXiv 2606.17519) — 49~741 툴에서 **7~85% 성능 저하**.
LiveMCPBench는 527 툴에서 **실패의 약 50%가 검색 오류**.

**The 99% Success Paradox** (arXiv 2605.18857) — 툴 정의만으로 5만 토큰 이상. 58개 툴이
55K 토큰. 후보가 많아지면 검색이 **무작위 선택과 같아지는 붕괴 구간**이 있다.

**SING: Synthetic Intention Graph** (arXiv 2606.16591) — 779 서버·7,471 툴에서
Recall@5 최대 **59.8% 향상**, 스키마 노출 **99.8% 감소**. one-shot 검색의 한계를 지적.

> **이비**: 지금 모듈 3개다. 이 문제는 아직 **없다**. 다만 우리 1단 모델은 후보 전체를
> 견주는 구조라 **모듈이 20~30개를 넘으면 그대로 붕괴 구간에 들어간다.**
> 임베딩 점수가 0.84~0.96 좁은 띠에 몰린다는 우리 실측이 이미 그 조짐이다.
> **대책이 필요해지는 시점**: 모듈 15개쯤. 그때 SING식 의도 그래프로 후보를 좁힌 뒤
> 점수를 매기는 2단 구조로 바꾼다.

### 온디바이스 자원 관리

**Agent.xpu: Efficient Scheduling of Agentic LLM Workloads on Heterogeneous SoC**
(arXiv 2506.24045) — 전경/배경 작업을 이기종 SoC(CPU·GPU·NPU)에 나눠 스케줄링.

**Parameter Efficiency Is Not Memory Efficiency** (arXiv 2604.22783) — 온디바이스
파인튜닝에서 파라미터 효율(LoRA 등)이 **메모리 효율과 다르다.**

**Energy-Efficient On-Device RAG on a Mobile NPU** (arXiv 2606.11257) — Snapdragon X Elite
기준 온디바이스 RAG 시스템 설계·벤치마크.

> **이비**: `engine.py`의 우선순위 큐(전경 > 배경)가 Agent.xpu의 문제의식과 같다.
> 우리는 한 모델만 올리는 단순화로 풀었고, 실측으로 상주 이득(3.8s → 0.34s)을 확인했다.
> **보강할 것**: 폰으로 갈 때 NPU 스케줄링이 필요해진다. 지금 파이썬 참조 구현엔 없다.

### KV 캐시 — 4GB VRAM의 실제 병목

**Towards Efficient LLM Serving: System-Aware KV Cache Optimization** (arXiv 2607.08057)
**RDKV: Rate-Distortion Bit Allocation for Joint Eviction and Quantization** (arXiv 2605.08317)
**UltraQuant: 4-bit KV Caching for Context-Heavy Agents** (arXiv 2606.20474)

KV 캐시가 **긴 문맥 서빙의 한계 요인**이고, 압축하면 같은 GPU에서 쓸 수 있는 문맥이
늘어난다. INT8은 오버헤드 6~58ms로 미미하다.

> **이비**: GTX 1050 Ti 4GB에서 Qwen2.5-VL-3B + mmproj가 안 들어가 CPU로 돌리는 중
> (42초). **이게 지금 제일 아픈 지점이다.** llama.cpp는 `--cache-type-k q8_0` 같은
> KV 양자화를 이미 지원한다 — `engine.py`에서 노출만 하면 된다. **바로 할 수 있는 개선.**

---

## 3. 음성 — 우리가 지금 씨름하는 것

### 지연 예산

실무 기준은 **P95 500ms 이하**, 1초는 상한선이다. 단계별로 STT·LLM·TTS·네트워크에
예산을 나눠 붙인다.

> **이비 실측**: 침묵 0.5s + 받아쓰기 0.4s = **체감 0.9~1.36초.**
> 업계 기준으로는 아직 느리다. 줄일 곳:
> - **침묵 대기 0.5초** — 가장 큰 덩어리. 아래 end-of-turn 논문이 답이다
> - 받아쓰기 0.4초 — 스트리밍으로 바꾸면 말하는 동안 미리 처리된다

### 말이 끝난 걸 더 빨리 아는 법

**Next-Turn: Duration-Aware Streaming Endpoint Detection via Time-to-Next-Speech-Onset
Prediction** (arXiv 2606.18094) — 고정 침묵 시간 대신 **다음 발화까지 남은 시간을 예측**해
끝점을 잡는다.

**Thai Semantic End-of-Turn Detection for Real-Time Voice Agents** (arXiv 2510.04016) —
소리만이 아니라 **문장이 끝났는지 의미로 판단**한다.

> **이비**: 우리는 지금 "0.5초 조용하면 끝"이라는 고정 규칙이다. 문장이 안 끝났는데
> 뜸 들이면 잘리고, 끝났는데도 0.5초를 기다린다. **의미 기반 끝점 판정**이 두 문제를
> 동시에 푼다 — 그리고 우리는 이미 1단 모델이 있어서 "이 문장이 완결됐나"를 물어볼 수 있다.

### 스트리밍 받아쓰기

**CarelessWhisper: Turning Whisper into a Causal Streaming Model** (arXiv 2508.12301) —
인코더를 인과적으로 바꿔 오디오를 증분 처리, 디코더는 부분 인코더 상태로 토큰 생성.

**Pushing the Limits of On-Device Streaming ASR** (arXiv 2604.14493, 2026-04) — Whisper·
Nemotron·Parakeet TDT·Canary·Conformer·Qwen3-ASR를 **50개 이상 설정으로** 배치/청크/
스트리밍 비교. 엣지 배포용 정확도·지연·메모리 균형을 체계적으로 정리.

**NPUsper: Eliminating Redundant Computation for Real-Time Whisper on Mobile NPUs**
(arXiv 2607.01108) — 폰 NPU에서 Whisper 실시간화.

> **이비**: 2604.14493이 **폰 앱 만들 때 모델 고르는 근거표**가 된다. 지금은 GPU small을
> 쓰지만 폰에는 안 들어간다. NPUsper는 그 답이 될 수 있다.

### Whisper의 무음 환각 — 우리가 실제로 겪은 것

우리 기록에서 조용한 구간에 `"여러분 안녕하세요… 시청해 주셔서 감사합니다"`가 나왔다.
공식 권고는 **`no_speech_threshold=0.6`과 `vad_filter=True`를 함께** 쓰는 것.

> **이비**: 우리는 VAD를 **껐다**(속도 때문에). 대신 `avg_logprob < -1.10`,
> `no_speech_prob > 0.60`으로 걸렀고 실측으로 갈렸다(멀쩡한 말 -0.38~-0.90 vs
> 지어낸 말 -1.46). **문헌 권고와 우리 실측이 같은 곳을 가리킨다.**

---

## 4. 반론 — 과설계를 막는 축

**In-Context Prompting Obsoletes Agent Orchestration for Procedural Tasks**
(arXiv 2604.27891, Dennis 외)

절차적 과제 3종(여행 예약·Zoom 지원·보험 청구)에서:

- 품질 **4.53~5.00** (오케스트레이션 4.17~4.84)
- 실패율 **0.5~11.5%** (오케스트레이션 **9~24%**)

절차를 통째로 시스템 프롬프트에 넣고 모델이 스스로 관리하게 하는 쪽이 낫다는 주장.
LangGraph·CrewAI를 직접 지목한다.

**LLM-Based Multi-Agent Orchestration: A Survey** (Future Internet 18(6):326) — Gartner는
**2027년까지 에이전틱 AI 프로젝트의 40% 이상이 비용과 불명확한 가치로 취소**될 것으로 전망.

> **이비**: 이 반론이 우리에게 **부분적으로만** 적용된다. 우리 오케스트레이터가 하는 일은
> "절차 관리"가 아니라 **단계 승격·계측·되묻기 판정**이다. 절차를 프롬프트에 넣는 것으로는
> "폰에서 실패 → PC로 승격"이 안 된다. 다만 경고로 새길 것:
> - **복합 지시(결정 13) 순차 체이닝**은 이 반론의 사정권이다. 여러 모듈을 엮는 절차를
>   우리가 관리할 게 아니라 모델에게 맡기는 편이 나을 수 있다.
> - 지금 모듈 3개에 계층을 더 쌓지 않는다.

---

## 5. 지금 당장 할 수 있는 것 — 우선순위

1. **KV 캐시 양자화** (arXiv 2607.08057, 2606.20474)
   `engine.py`에 `type_k`/`type_v`를 열어 q8_0으로. 4GB에 3B 비전 모델이 들어갈 수 있다.
   **CPU 42초 → GPU 몇 초**가 걸린 문제라 효과가 제일 크다.

2. **스킬 심사 자동화 1단계** (arXiv 2608.05810)
   구조 타당성 심사는 기계가 한다: 없는 모듈 참조·순환·중복 트리거·기존 스킬과의 충돌.
   사람은 의미 판단만. **스킬 풀 크기 상한**도 같이 둔다(상전이 대비).

3. **의미 기반 끝점 판정** (arXiv 2606.18094, 2510.04016)
   침묵 0.5초를 "짧은 침묵 + 문장 완결 판정"으로 대체. 1단 모델이 이미 있으니 추가 모델
   없이 된다. **체감 지연에서 가장 큰 덩어리를 줄이는 길.**

4. **항목에 출처 달기** (arXiv 2608.01708 PGMem)
   `Note`에 `from_instruction` 필드. "이 기억이 어디서 왔나"를 되짚을 수 있어야
   잘못 배운 것을 찾아 지운다.

5. **라우팅 2단 구조는 아직 하지 않는다** (arXiv 2606.16591)
   모듈 15개가 되면 그때. 지금 하면 과설계다 — 반론 논문이 경고하는 바로 그것.

---

## 읽는 순서

설계를 바꾸려면: **2608.05810**(승인 게이트 근거) → **2607.08010**(스킬 컴파일) →
**2506.02153**(SLM 우선) → **2604.27891**(반론).

성능을 바꾸려면: **2607.08057**(KV 캐시) → **2606.18094**(끝점 판정) →
**2604.14493**(폰 ASR 모델 선택).
