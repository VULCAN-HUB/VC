# ★물음 — v0.1.84 API 정상인데 엔진 상태 조회 실패 표시
일반 실행, 제공GGUF를 exe옆models에 배치. 인증된 AI검색/부분조회/전체조회는200이지만 GUI 상단은 '엔진 — 서버에 못 물어봤다'입니다. 보안창 없이 VC 화면이 관측됐고 재관측에도 동일합니다. 지시서05에서 별도결함이라고 한 조건이 재현됐습니다. 엔진 상태 조회 경로/응답처리를 확인해 주세요. 코드수정은 하지 않았습니다.

추가 참고: 직접 복사한3069자 원문은4개H2와1개H1을 포함해 headings5개, 부분775자 조회 성공. 앞머리 없는 원문이라 적합률4/5로 내려갔으며 자동으로 앞머리가 생긴다고는 가정하지 않았습니다. 기존4개는100% 유지. 맞짝은 벡터완성 후1개(서로다른두글)라 이전 벡터미완성 상태의0과 직접 비교하지 않았습니다.
