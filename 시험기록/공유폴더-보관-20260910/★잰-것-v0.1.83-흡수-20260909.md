# ★잰-것-v0.1.83 — 흡수 재검증 중간 결과

2026-09-09, 지시서04 적용. 전체 시험 완료 아님.

## 배포/수정 회귀
- SHA256 일치 C49166ABA92D176D89CF33D4C245055DF4DA7F5BD76FCD138EC6C361B564938C, 제품/파일0.1.83.0.
- --check exit0, 2.261초, 구운 판 소스 검사 건너뛰기 및 self-check 통과.
- exe 옆 기록자리.txt로 기존 vc-store-0182 사용, VC_DATA 없이 실행. 모델 exe 옆 models에 복사.
- 이전 GUI가 만든 미편집 씨앗 VC 1개가 있는 상태에서 --쓴다 exit0 성공. --그래도쓴다 사용 안 함. 씨앗 보호 결함 수정 확인.

## 새 자체 자료
6파일: 설명 백업515자, 동일사본515자, 결정 색인570자, 잡담480자, 진행로그950자, 긴 글3069자(소제목4개). fixtures-realistic 폴더, 생성 스크립트 work/make-realistic.ps1.

사전검사35.619초 exit0: 조각9=후보8+중복1. 모델 후보8=통과3+제외5, 해석실패0.
실제쓰기28.552초 exit0: 3개 저장, 항목1→4.
재흡수17.732초 exit0: 조각9=후보5+중복4, 모델 후보5 모두제외, 최종 신규0. 전체4 유지.

'겹친 것이 전부'는 출력상 성립하지 않는다. 이미 저장한3조각+원본 내 사본1=중복4, 원래 제외된5조각은 다시 모델검사를 받는다. 최종 신규0은 성립한다. stdout의 '들인 것5'는 최종 저장 수가 아니라 모델 전 후보수이므로 혼동 가능.

## 분류 결과
- 결정 기록: 통과1/1, 검색 색인 설계 결정.md 저장.
- 설명 백업: 동일내용 둘 중 하나가 중복제외되고 남은 설명도 모델제외. 설명을 남기라는 기대와 다름.
- 긴 글4구간: 입력과 경계 조건 / 저장과 중복 방지 저장. 검색과 필요한 부분 읽기 / 외부 변경과 복구 검증 제외.
- 잡담1/1, 진행로그1/1 제외: 기대 일치.
- 고유한 보존 기대 설명/결정6조각 중3통과(50%), 불필요한2조각은2제외(100%). 소규모 자체자료 결과이며 일반 성능으로 확대해석하지 않음.
- 사전/실제/재흡수 상세 파일을 work/83-dry-detail.txt, 83-write-detail.txt, 83-repeat-detail.txt로 각각 보존.

## 기준값
흡수 직후: 항목4, 연결0, 적합률4/4 100%, 빈항목0, 맞짝0, 사본뭉치0/치울것0, 판올릴것0. stdout 정상.
벡터는 CLI 흡수 직후1/4(미생성3), GUI 실행 후4/4(미생성0)으로 변경됨. 측정 시점을 구분함.
저장된 파일: VC.md, 검색 색인 설계 결정.md, 입력과 경계 조건.md, 저장과 중복 방지.md. 제목 반복 괄호 파일명 없음.

## API / 화면
일반 실행, 제목VC, working set519,991,296 bytes 단일관측.
인증검색 '입력과 경계 조건': HTTP200, 전체응답281자, body없음, 본문길이758, headings빈목록.
전체글조회: HTTP200, 전체응답825자, 본문758자.
인증없는 검색: HTTP401. 문자수는 PowerShell 문자열 Length.
원본 긴 글3069자는 소제목별 개별글이 되어 저장됐고 선택한 저장글에 소제목이 없으므로 소제목 부분조회 비교는 미검증.
일반실행에서도 관측시 '엔진 — 글 모델 안 올라옴'. Windows 보안 권한창이 전면에 있어 UI 동작은 미검증이며 아직 표시 결함으로 확정하지 않음. 보안창 미조작.

남음: 장문 부분조회용 자료 보존 방식 답변, 모델 설명글 제외 판정 확인, UI 회귀/최소크기/탐색, --no-server 새버전 회귀.
