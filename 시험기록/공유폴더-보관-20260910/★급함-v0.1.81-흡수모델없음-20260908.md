# ★급함 — v0.1.81 흡수 기준값 측정 중단: .gguf 모델 없음

10개 Markdown 테스트 파일을 생성했습니다. 여러 소제목의 긴 글, 소제목 없는 글, 빈 파일, 앞머리만 있는 파일, 동일 내용 2개, 상호 링크, 한글/공백/괄호 파일명을 포함했습니다.

`VC.exe --흡수 <테스트 자료 폴더>` 결과: 종료 코드 1, 소요 0.338초.
출력: 「비싼 문지기를 못 돌렸다: RuntimeError: 글 모델이 없다. …\_internal\models 에 .gguf 를 넣어라 (내려받는 자리는 지시서에)」 및 「문지기 없이 쌓으면 과정 소음이 그대로 항목이 된다. 안 쓴다.」
지시서 01/02에는 모델 다운로드 위치/파일명이 없습니다. 본 것·들인 것·겹친 것·버린 것 집계도 출력되지 않았습니다. --쓴다 실행 및 흡수 이후 기준값 측정은 이 문제 해결까지 중단합니다.

필요한 정확한 모델 파일/배치 방법 또는 수정 배포본을 알려 주세요. 다운로드를 추측하거나 임의 모델로 대체하지 않겠습니다.

확인된 정상 사항: SHA256 일치, 버전 0.1.81.0, VC_DATA로 별도 창고 지정 성공, GUI 전 항목 0, GUI 후 씨앗 항목 1 및 판 적합률 1/1 (100.0%), 본문 빈 항목 0. GUI 제목 VC, 메모리 약 504,037,376 bytes(단일 시점 관측).
