# ★잰-것-v0.1.86 — 휴지통 중간 검사
2026-09-10. 지시서06~08 반영. 전체 UI 탐색은 아직 완료하지 않았다.

## v0.1.84 반복성 마무리
보존된 두 dry-run 상세 결과에서 시간 줄만 제외하고 비교한 차이0. 후보8, 중복1, 모델통과3/제외5와 목록 동일. 1회29.646초, 2회 상세기록27초(정밀시간 로그 미회수). 이 두 실행의 반복성은 확인했으나 기계간 차이 원인은 이 검사로 입증하지 않는다.

## 배포/환경
SHA256 9E5CA31A2FAC38E912390D4825E350DE6964A440F1E4F33D5568B53FA9566C16 일치, 제품0.1.86.0. --check exit0 2.447초.
기존 창고 보존. 별도 work/vc-store-0186 사용, exe옆 기록자리.txt. 제공GGUF는 exe옆models, 기존 씨앗 VC.md 사본을 새 글폴더에 준비했다. 초기 CLI 색인이 비어 실제흡수 stdout은0→3으로 표시됐다. GUI후 씨앗포함4, 적합률4/4, 벡터4/4. 따라서 이 실행만으로 색인에 씨앗이 이미 있는 경우의 보호회귀를 단정하지 않는다.

## 휴지통
- 첫 --흡수 --쓴다: exit0,25.092초. 조각9,후보8,중복1,저장3. 제외5개가 휴지통에 저장됐다는 문구 확인.
- --휴지통: exit0,0.285초,담긴것5.
- 파일5개: 검색과 필요한 부분 읽기 / 로컬 문서 백업 절차 / 외부 변경과 복구 검증 / 잡담 인사 / 진행 로그 (파일명에 지문접미사).
- 앞머리 표본: kind휴지통, 지은이문지기, 출처, 줄, 버린날, 까닭, 지문 존재. 나머지4개 모든필드 전수확인은 미실시.
- 같은폴더 재흡수: exit0,0.312초,후보0/중복9,새저장0. 모델재심사 출력없음. 이전 v84 재검사18.038초에 비해 약57.8배 짧은 관측. 동일실행환경의 통계적 벤치마크는 아님.
- 인증 API로 '반갑습니다' 검색: 복원전 results=[] 확인. GUI검색 직접 실행은 아직 미검증.
- 지시서대로 휴지통의 잡담 인사 파일1개를 같은 시험창고 글폴더로 이동. 절대경로가 시험창고 아래인지 검증 후 이동,전후SHA256일치.
- 복원후 같은검색에 해당글1개(본문480자)가 노출. 항목4→5,벡터5/5. kind는휴지통으로 유지,적합률4/5(복원파일에 스키마없음). 되살리기 동작 확인. 휴지통 비우기/파일삭제 없음.

## 엔진/모델 UI
일반실행 제목VC. 이전 '서버에 못 물어봤다' 대신 '엔진 local · 모델 없음' 표시.
모델패널은 열리고 GPU 정보·모델선택 컨트롤이 표시됨. 글자 항목은 '자동 (없음)', GGUF 목록은 비어 보임.
실제모델: work/VC-v0.1.86/models/qwen2.5-1.5b-instruct-q4.gguf (1,117,320,736 bytes).
설정 backend.kind=local, model_dir는 work/VC-v0.1.86/_internal/models. CLI는 모델로 분류했으나 서버/GUI는 발견하지 못함. 설정경로 불일치를 확인했고 임의 설정변경은 하지 않았다. 별도 질문 참조.

남음: 장문 API 새판회귀,긴글목차UI,최소크기,링크/뒤로/외부수정회귀,30분 탐색,일반GUI검색 제외 확인,모델경로 답변. 기존결과를 새판통과로 간주하지 않음.
