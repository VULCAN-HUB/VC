# ★잰 것 v0.1.88 — 2026-09-12

## 1. 시험표 요약

- [잰 것] 배포 ZIP의 SHA-256은 지시서 값과 같다.
- [잰 것] v0.1.87이 있던 `C:\VC\app`에 v0.1.88을 덮어 풀었다. `models\` 1개와 `기록자리.txt`는 덮어 풀기 전후 해시·개수가 같아 그대로 남았다.
- [잰 것] 처음 정상 실행에서 Windows 방화벽 창은 뜨지 않았다. 실행 직전 기준점 뒤 생긴 방화벽 이벤트 2097/2099를 마지막에 다시 확인하니 8건이 있었지만, 규칙 이름과 응용 프로그램 경로가 모두 Claude 쪽이었다. VC 규칙은 0건이다. 보안 창은 누르지 않았다.
- [잰 것] 새 빈 창고 + `fixtures-realistic` 사본 `--쓴다`: 종료값 0, ✘ 0. 남긴 것 3·버린 것 5, 다시 세기 후보 0·겹친 것 9, AI 1단 283자·한 편 625자·full=1 854자로 만든 쪽과 같다.
- [잰 것] long 창고, 폴더 없이 `--시험표`: 종료값 0, ✘ 0. 새 항목인 2단 한 토막은 `입력과 경계 조건` 831자로 나와 지시서 값과 같다. 한 편도 3226자로 같다.
- [잰 것] long 창고의 1단/full=1은 542/3713자로, 기준 356/3457보다 각각 186/256자 크다. long 창고의 `VC.md`에 `[[긴 글 검증 절차]]` 줄이 실제로 남아 있다.
- [짐작이다] 위 1단/full 증가분은 `현재상태.md`가 미리 적어 둔 대로 그 추가 링크 줄이 같은 물음말에 걸린 결과다. ✘은 아니지만 기준과 다른 값이라 적는다.
- [잰 것] 화면 메모리는 기본 창고 497MB로 만든 쪽 491MB보다 6MB 컸고, long 창고는 489MB였다. 최소 창 1074x430, 최상위 창 1개, 엔진 `local · 모델 1개 대기`는 정상이다.

## 2. 따로 잰 값

- [잰 것] 새 창고의 `data\notes`를 파일시스템에서 직접 세었다. 기본 `VC.md`와 서식 파일을 빼면 흡수 문서 3개이고, `휴지통` 파일은 5개다. 시험표의 `3개 썼다`·`담긴 것 5개`와 같다.
- [잰 것] long 창고의 `긴 글 검증 절차.md`를 PowerShell `Get-Content -Raw -Encoding UTF8`의 문자열 길이로 직접 세어 3069자였다. `현재상태.md`의 안 바뀔 값과 같다.

## 3. 지시서의 판단 몫 결과

- [잰 것] 통과. `긴 글 검증 절차`를 열고 목차에서 `저장과 중복 방지`를 골랐더니 그 소제목으로 이동했다.
- [잰 것] 이어 `가리킨 곳`의 `VC`를 열고, 본문 `[[긴 글 검증 절차]]` 링크를 눌렀더니 긴 글로 돌아왔다.
- [잰 것] 긴 글의 `←`를 눌렀더니 직전 `VC`로 돌아왔다. 앞으로 화살표도 활성화됐다. 판단 시험 뒤 창은 정상 종료했고 `끔 (0)`을 확인했다.

## 4. 못 잰 것 / 짐작인 것

- [잰 것] 창 관찰 도구로 EXE를 직접 처음 띄운 한 번은 작업 폴더가 EXE 옆이 아니어서 상대경로 `data` 접근 거부로 시작하지 못했다. `C:\VC\app`을 작업 폴더로 명시한 정상 실행에서는 창이 떴고 정상 종료값 0이었다. 정상 배포 실행의 결함으로 세지 않았다.
- [못 쟀다] 한글 입력기 조합 중 동작, 30분 연속 사용은 이번 지시서의 판단 몫 밖이라 재지 않았다.
- [짐작이다] 메모리 6MB 차이는 실행 때 변동일 수 있다. 반복 측정은 하지 않았다.

쓴 토큰: 못 쟀다
