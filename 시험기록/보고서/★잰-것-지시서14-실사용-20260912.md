# ★잰-것-지시서14-실사용-20260912

## 시험 범위와 시간

- [잰 것] 별도 시험 창고 `C:\VC\실사용-v0190-20260912-0949` 한 곳에서만 사용했다. 원래 long 창고는 건드리지 않았다.
- [잰 것] `vc-기록.log` 기준 창은 2026-09-12 09:50:27에 떴고 10:20:51에 정상 종료되어 30분 24초 사용했다.
- [잰 것] 새 글 2개(`실사용 메모`, `회의 준비 기록`)를 쓰고, 링크가 만든 글의 제목을 `연결 메모`에서 `연결 메모 수정`으로 바꿨다. 끝날 때 항목 4, 연결 3이었다.

## 실제로 해 본 것

- [잰 것] 글 작성·본문 수정·제목 변경·태그 추가(`#실사용`, `#검증`, `#회의`, `#연결`, `#마침`)가 저장됐다. 제목 변경 뒤 실제 파일 이름과 기존 `[[링크]]` 표시도 `연결 메모 수정`으로 따라 바뀌었다.
- [잰 것] `[[연결 메모]]`를 누르자 없는 글이 만들어졌고, 새 글에서 `[[실사용 메모]]`로 다시 연결했다. 링크와 가리킨 곳 목록으로 왕복했다.
- [잰 것] ←와 →로 `실사용 메모`와 `연결 메모 수정` 사이를 왕복했다.
- [잰 것] `회의 준비 기록`에 4개 목차(`회의 준비`, `안건`, `결정`, `다음 행동`)가 잡혔고 `결정` 목차 항목을 눌렀다.
- [잰 것] `tag:실사용`은 관련 글 2개, 따옴표 검색 `"링크 왕복"`은 한 글, 없는 말 `없는말실사용`은 뜻으로 가까운 글 4개, `외부 변경`은 관련 글 4개로 나왔다.
- [잰 것] 검색 실행 뒤 접근성 상태가 돌아오기까지 `외부 변경`은 약 0.287초, `tag:마침`은 약 0.196초였다. 도구의 상태 읽기까지 포함한 값이다.
- [잰 것] 글을 곁에 띄우고 닫았고, 활동 패널을 펼쳐 보았다. 창 최대화는 제목 표시줄 단추가 `복원`으로 바뀐 것으로 확인했다.
- [못 쟀다] 최대화 뒤 원래 크기 복원은 단추를 눌렀지만 상태가 계속 `복원`으로 보여 성공했다고 확인하지 못했다.
- [잰 것] 편집 중인 `연결 메모 수정`을 파일에서 바꾸자 VC가 외부 변경을 즉시 알아차리고 현재 입력을 우선하며 외부 내용을 `지난 판`에 둔다고 말했다.
- [잰 것] 읽기 상태의 `실사용 메모`를 파일에서 바꾼 뒤 `읽기 화면`을 검색하자 밖에서 추가한 문장이 검색 결과에 잡혔고, 편집 화면을 열었을 때 본문에도 그대로 보였다.

## 손이 멈춘 자리 / 못 찾은 것 / 무서웠던 것

- [잰 것] 검색칸과 제목칸이 접근성 트리에서 둘 다 `편집`으로만 보여 자동 조작 중 초점이 어디인지 구별하기 어려웠다. 실제 제목 변경은 됐지만, 검색은 `Ctrl+O`를 다시 눌러야 확실했다. 사람 눈으로는 칸이 보이므로 VC 자체 결함이라고 단정하지 않는다.
- [잰 것] `⋯` 메뉴는 열어 보았으나 삭제는 실행하지 않았다.
- [못 쟀다] 글 삭제와 휴지통 복원은 Windows 화면 자동화 안전 규칙상 로컬 삭제 직전에 별도 사용자 확인이 필요해 이번 무인 판에서는 하지 않았다.
- [못 쟀다] 옵시디언과 같은 조건으로 나란히 비교하지 않아 “옵시디언에서는 되지만 VC에서는 안 되는 것”은 확인하지 못했다.

## 말이 어색하거나 거짓말하는 자리

- [잰 것] 외부 변경 알림이 `연결 메모을 밖에서도 고쳤어.`라고 말했다. `연결 메모를 밖에서도 고쳤어.`가 자연스럽다.
- [잰 것] 따옴표 검색 뒤 `"링크 왕복"는 연결 메모 하나야.`라고 말했다. 끝말 `왕복`에 받침이 있으므로 `"링크 왕복"은`이 자연스럽다.
- [잰 것] 태그 검색 뒤 `'tag:마침'는 실사용 메모 하나야.`라고 말했다. 끝말 `마침`에 받침이 있으므로 `'tag:마침'은`이 자연스럽다.
- [짐작이다] 조사 선택이 따옴표나 `tag:`가 붙은 검색어의 끝말 받침을 보지 않고 고정된 `는`을 붙이는 듯하다.

## 느린 자리

- [잰 것] 작성, 저장, 링크 이동, 뒤·앞 이동, 곁창 열기, 검색은 체감상 바로 반응했다. 1초 이상 기다렸다고 느낀 조작은 없었다.

## 스크린샷 사용

- [잰 것] 초반 모양과 편집 초점을 확인하면서 스크린샷 7장을 요청해 지시서 상한 5장을 2장 초과했다. 시험자 실수다. 그 뒤에는 접근성 트리만 사용했다.

## 정상 종료와 죽음 기록

- [잰 것] 정상 종료 뒤 `vc-기록.log` 마지막 줄은 `2026-09-12 10:20:51  끔 (0)`이었다.
- [잰 것] `VC.exe --report`로 `VC-진단-20260912-102113.zip`을 만들었다. `무엇이 어디.json`의 죽음 기록은 `비치명 COM 예외 1줄 · 그 밖의 네이티브 예외 0줄 · 파이썬 traceback 0개`였다.
- [잰 것] `vc-죽음.log`를 직접 세어 `code 0x8001010d` 1줄, 전체 `Windows fatal exception` 1줄, traceback 0개였다. 파일 마지막 수정 시각은 시작 직후인 09:50:28이어서 30분 사용 중 새로 자란 줄은 없었다.
- [짐작이다] 없음. 죽음 기록의 COM 줄은 진단 묶음 분류를 그대로 옮겼다.
