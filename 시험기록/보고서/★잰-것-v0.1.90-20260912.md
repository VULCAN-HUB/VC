# ★잰-것-v0.1.90-20260912

## 1. 시험표 요약

- [잰 것] `VC-v0.1.90.zip` SHA-256은 선언값과 실제값이 모두 `12D28C6084313C6E2E3FC5FDE7D20E32336E38A6158FB53C335928997E4BBB4E`로 같았다.
- [잰 것] 기존 `C:\VC\app`에 덮어 풀었고, exe 옆 `models\`와 `기록자리.txt`는 남았다.
- [잰 것] 새 빈 창고 `C:\VC\시험-v0190-20260912`와 fixtures 사본 `C:\VC\자료-v0190-20260912`로 `--시험표 --쓴다`를 실행했다. 프로세스 종료값은 0이었다.
- [잰 것] `★시험표-v0.1.90.md`에 ✘ 줄은 0개였다. `2단 한 토막`은 소제목 있는 글이 없어 `-`로 기록됐으며 실패가 아니다.
- [잰 것] 주요 값은 남긴 것 3, 버린 것 5, 다시 세기 후보 0, 겹친 것 9, 휴지통 5, AI 1단 283자, 글 한 편 625자, full=1 854자, 화면 메모리 494MB였다.
- [잰 것] 시험표 원본을 공유폴더에 `★시험표-v0.1.90.md`로 올렸다.

## 2. 따로 잰 값

### 죽음 기록 가르기

- [잰 것] long 창고를 기록 자리로 두고 v0.1.90 `VC.exe --report`를 실행했다. 만들어진 `VC-진단-20260912-033231.zip`의 `무엇이 어디.json`에서 죽음 기록은 `비치명 COM 예외 11줄 · 그 밖의 네이티브 예외 1줄 · 파이썬 traceback 2개`였다.
- [잰 것] long 창고의 `vc-죽음.log`를 `Select-String`으로 직접 세었다. `Windows fatal exception: code 0x8001010d` 11줄, 전체 `Windows fatal exception:` 12줄이어서 그 밖의 네이티브 예외 1줄, `Traceback (most recent call last):` 2개였다.
- [잰 것] 직접 센 값 11·1·2는 진단 묶음의 값 11·1·2와 모두 같았다.

### 시험표와 별도 확인한 값 하나

- [잰 것] 새 시험 창고의 `휴지통` 아래 실제 파일을 PowerShell로 재귀 집계하니 5개였다. 시험표의 `--휴지통` 5개와 같았다.

## 3. 지시서의 판단 몫 결과

- [잰 것] long 창고를 기록 자리로 둔 실제 v0.1.90 창에서 `Ctrl+O`로 검색칸에 갔다.
- [잰 것] 어느 글에도 없는 `어디에도없는말`을 검색하니 말풍선은 `'어디에도없는말'이 든 글은 없어. 뜻으로 가까운 것 5개야.`였다. 검색어 끝에 받침이 있어 `이 든`을 썼고, 지시된 새 문구와 일치했다.
- [잰 것] 이어서 있는 말 `검증`을 검색하니 말풍선은 `'검증' 관련 5개야. 더 좁히면 내용을 보여줄게.`였다. 낱말이 걸릴 때의 예전 `관련 N개야` 꼴과 일치했다.
- [잰 것] 두 검색 모두 실제 창 화면과 접근성 트리에서 확인했다. 사용한 화면 캡처는 결과 확인용 2장이다.
- [잰 것] 판단 몫을 마친 뒤 `Alt+F4`를 보내고 열린 창 목록을 다시 확인했으며 VC 창은 없어졌다.

## 4. 못 잰 것 / 짐작인 것

- [못 쟀다] 없음. 이번 지시서가 요구한 자동 시험표, 죽음 기록 직접 집계, 추가 독립 측정, 실제 창 검색 문구를 모두 쟀다.
- [짐작이다] 없음. 위 수치는 모두 파일·프로세스·직접 집계로 잰 값이다.
