# ★잰-것-v0.1.94-20260912

## 1. 시험표 요약

- [잰 것] `VC-v0.1.94.zip` SHA-256은 `901BF43C0D9A524942CB1F924BA35E60D851A14AE53021873D922E035D8DA998`로 지시서 및 `.sha256`과 같았다.
- [잰 것] `C:\VC\app`에 덮어 푼 뒤 `기록자리.txt` SHA-256은 전후가 같았다. ZIP에는 `models`와 `기록자리.txt` 항목이 없었다.
- [잰 것] 새 격리 저장소와 해시가 같은 입력 사본 6장으로 시험표를 돌렸다. 종료값 0, `✘` 0개였다.
- [잰 것] 시험표 `--doctor` 줄은 `판 v0.1.94`부터 적혔다.
- [잰 것] 남긴 것 3, 버린 것 5, 다시 세기 후보 0, 겹친 것 9, 휴지통 5였다.
- [잰 것] 1단 search 283자, 한 편 625자, full=1 854자였다. 소제목 있는 글이 없어 2단 한 토막은 시험표에서 하지 않았다.
- [잰 것] 화면 자동 측정은 창 `VC`, 최상위 창 1개, 항목 4, `엔진 local · 모델 1개 대기`, `★ 원격 열림 :8765`였다.

## 2. 따로 잰 값

- [잰 것] 시험 저장소의 `휴지통` Markdown 파일을 PowerShell로 따로 세니 5개였다. 시험표의 5개와 같았다.
- [잰 것] 공유폴더 원본 6장과 시험 입력 사본 6장을 파일 이름과 SHA-256으로 대조했다. 차이는 0개였다.

## 3. 급함 다시 재기 — `--no-ui` 뜻 검색

- [잰 것] 별도 새 저장소에서 `VC.exe --no-ui`를 켜고 새 글 `등대 항해 안내` 한 장을 넣었다. 본문은 `안개 속 등대가 선박을 인도한다.`였다.
- [잰 것] 1분 기다린 뒤 Bearer로 `/eb/v1/memory/search`를 직접 불렀다.
  - `야간 항해를 돕는 해안 표지`: HTTP 200 · 1건 · `등대 항해 안내` 1등.
  - `안개 속 등대가 선박을 인도한다`: HTTP 200 · 1건 · `등대 항해 안내` 1등.
  - `lighthouse`: HTTP 200 · 1건 · `등대 항해 안내` 1등.
- [잰 것] v0.1.93에서 0건이던 뜻 검색은 이 자리의 v0.1.94에서는 세 물음 모두 1건으로 나왔다.
- [못 쟀다] `--no-ui`의 stdout/stderr를 파일로 받았지만 `[뜻 벡터]` 줄은 남지 않았다. 서버 기록에는 `서버 열었다 0.0.0.0:8765`만 있었다. 세 알림 중 무엇이 화면 콘솔에 보였는지는 이번 방법으로 못 쟀다.
- [잰 것] 시험 뒤에는 그 `--no-ui` VC 프로세스 하나만 끝냈고, 남은 `VC.exe` 프로세스는 0개였다.

## 4. 판단 몫

- [짐작이다] `--doctor`에는 벡터 개수와 함께 **같은 검색 서비스가 실제로 읽는 `Notes` 인스턴스**로 `semantic()` 왕복을 한 번 시키는 값이 알맞다.
- [짐작이다] 검사 글 한 장을 골라 그 글의 본문 일부를 질의로 쓰되 **낱말 검색은 끄고 뜻 검색만** 실행해야 한다. 혼합 검색이면 임베더가 빠져도 낱말 일치가 결함을 가릴 수 있다.
- [짐작이다] 성공 줄은 예를 들어 `뜻 검색 왕복 ✔ · 검사 id <id> · 제 글 1등 · score <값>`처럼 적고, 결과가 없거나 다른 글이면 `✘`, 항목 0개나 모델 없음이면 `못 잼`으로 갈라야 한다.
- [짐작이다] 이 검사는 배선·캐시·벡터 읽기를 확인하는 작은 자기일치 검사다. 뜻 품질 자체는 보증하지 않으므로, 바꿔 말한 문장들의 품질은 기존 `--찾기점수`가 따로 맡는 편이 낫다.
- [잰 것] 이 판단에는 화면 모양을 볼 일이 없어 스크린샷을 쓰지 않았다.

## 5. 못 잰 것 / 짐작인 것

- [못 쟀다] `[뜻 벡터]` 콘솔 알림 문구.
- [짐작이다] `--doctor` 왕복 검사 설계는 제안이며, 프로그램에 구현해 직접 돌린 결과가 아니다. 코드는 고치지 않았다.
