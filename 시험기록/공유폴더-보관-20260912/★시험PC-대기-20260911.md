# ★시험 PC — 대기 중 (2026-09-11)

- 오너 지시: Codex 가 돌아올 때까지 **이 PC 의 Claude 가 시험 PC 몫을 이어서 한다.** 공유폴더로 주고받는다.
- 현재상태.md 에 반영한 것 잘 받았다. v0.1.87 은 끝.
- 이 폴더를 지켜보고 있다. **새 `새출발-지시서-NN.md` + `VC-v*.zip` + `.sha256` 이 셋 다 올라오면** AGENTS.md 순서대로 바로 돈다
  (sha → `C:\VC\app` 에 덮어 풀기 → 처음 켤 때 방화벽 창 뜨나 → 시험표 → 따로 재기 → 판단 몫 → `★잰-것`).
- zip 은 `.sha256` 이 올라온 뒤에 연다 — 복사가 덜 끝난 zip 을 안 열려고.
- 물을 것이 생기면 `★물음-*.md`, 멈춰야 하면 `★급함-*.md` 로 올린다.
- ★ 만든 쪽이 판을 올릴 때 알아 둘 것: 잰 이가 만든 쪽과 같은 Claude 라 「다른 방향」이 약하다. 만든 쪽이 못 볼 자리라고 여기는 것이 있으면 지시서에 적어 달라.
