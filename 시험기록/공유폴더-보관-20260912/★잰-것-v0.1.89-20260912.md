# ★잰-것-v0.1.89 — 2026-09-12

## 1. 시험표 요약

- [잰 것] `VC-v0.1.89.zip` SHA-256은 `76CB3727118112AAFB49B8C3680D4AFD5470A5C000F3D13EFB951C2BC8111B4F`로 지시서와 같다.
- [잰 것] `C:\VC\app`에 `Expand-Archive -Force`로 덮어 푼 뒤 `기록자리.txt`의 SHA-256과 `models` 안 파일 1개의 경로·크기·SHA-256은 전과 같다.
- [잰 것] 새 빈 창고와 fixtures 사본 6개를 쓰고 `--쓴다`로 돌린 공식 시험표의 종료값은 0이고 ✘ 줄은 0개다.
- [잰 것, 만든 쪽 도구가 잰 값] 남긴 것 3 · 버린 것 5 · 못 알아들은 것 0 · 다시 세기 후보 0 · 휴지통 5다.
- [잰 것, 만든 쪽 도구가 잰 값] `--check` 종료값 0, 검색은 1단 283자 · 한 편 625자 · `full=1` 854자다.
- [잰 것, 만든 쪽 도구가 잰 값] 실제 창은 `VC`, 엔진은 `local · 모델 1개 대기`, 원격은 `:8765`, 메모리는 499MB다.
- [잰 것] ✘ 줄이 없어서 따로 가를 원인은 없다.

## 2. 따로 잰 값

- [잰 것] PowerShell `Get-ChildItem -File -Recurse`로 새 시험 창고의 `휴지통`을 직접 세니 5개다. 시험표의 5와 같다.
- [잰 것] 시험 뒤 공유폴더 원본 fixtures 6개와 시험 입력 사본 6개를 파일 이름별 SHA-256으로 직접 견주니 6개 모두 같다. 시험표의 “원본 파일은 하나도 안 건드렸다”와 같다.

## 3. 지시서의 본론과 판단 몫

- [잰 것] `Start-Process C:\VC\app\VC.exe -WorkingDirectory C:\Windows\System32`로 띄우니 실제 프로세스의 주 창 제목이 `VC`였고 실행 중이었다.
- [잰 것] 위 실행 전과 실행 뒤 모두 `C:\Windows\System32\data`는 없었다. v0.1.88의 “딴 작업 폴더에서 안 뜸”은 이 방법으로 재현되지 않았다.
- [잰 것] 그 실행에서 보안·방화벽 창은 보이지 않았고, VC 창은 정상 닫기로 끝났다.
- [잰 것] 창 관찰 도구가 `C:\VC\app\VC.exe`를 직접 띄우게 한 방법으로도 제목 `VC`인 창이 하나 떴다. 접근성 트리와 화면에서 `엔진 local · 모델 1개 대기`, `★ 원격 열림 :8765`를 확인했고 정상 닫기로 끝났다.
- [잰 것] `VC.exe --doctor` 종료값은 0이고 새 `vc-진단.json`의 `목소리 모델 있나`는 `true`다. 같은 파일의 `모델 자리`는 `C:\VC\app\_internal\models`다.
- [잰 것] 이번 지시서에는 별도의 판단 몫이 없다.

## 4. 못 잰 것 / 짐작인 것

- [못 쟀다] 창 관찰 도구가 직접 띄운 두 번째 실행의 내부 작업 폴더 값 자체는 도구가 내놓지 않아 수치로 확인하지 못했다. 다만 그 방법으로 실제 VC 창이 뜬 것은 화면과 접근성 트리로 쟀다.
- [잰 것] 처음 만든 별도 시험 경로 하나는 fixtures 복사 명령 오류로 입력 0개인 시험표가 생겼다. 공식 결과에서 제외했고, 경로를 새로 만들어 fixtures 6개를 확인한 뒤 다시 돌렸다. 제외한 경로와 결과는 지우지 않았다.
- [짐작이다] 없음.
- [못 쟀다] 쓴 토큰 수는 이 환경에서 보이지 않는다.
