# v0.1.89 — **딴 작업 폴더에서 켜기**가 본론이다

```
VC-v0.1.89.zip
SHA-256  76CB3727118112AAFB49B8C3680D4AFD5470A5C000F3D13EFB951C2BC8111B4F
```

AGENTS.md 차례대로 돈다. v0.1.88 보고 잘 받았다 — 방화벽까지 전부 통과로 닫았다.

★ 네가 「정상 실행의 결함으로 세지 않았다」고 적은 것(작업 폴더가 exe 옆이 아니면 안 뜬다)이
**진짜 결함이었다.** 바로가기의 「시작 위치」가 다르거나 다른 프로그램이 VC 를 부르면 사람도 밟는다.
원인: 서버가 결과물 폴더를 **작업 폴더 기준 `data\artifacts`** 로 만들다 접근 거부로 죽었다.
쫓다가 같은 모양 둘을 더 찾았다 — 녹음 폴더, 그리고 **목소리 모델**(구운 판에서 늘 못 찾고 윈도 기본 목소리로 내려앉았을 것이다).

---

# 1. 새로 든 것

```
딴 작업 폴더에서 켜도 뜬다 — 결과물·녹음 폴더를 기록 자리 기준으로
목소리 모델을 모델 자리 아래 piper 에서 찾는다
--doctor 에 「목소리 모델 있나」
```

## 만든 쪽에서 구운 판으로 잰 것

[잰 것, 만든 PC · 구운 v0.1.89]

```
작업 폴더 C:\Windows\System32 에서 --화면상태   종료값 0 · 창 「VC」 · 엔진 「local · 모델 1개 대기」
                                             · System32\data 안 생김   (v0.1.88 은 여기서 안 떴다)
작업 폴더 C:\Windows\System32 에서 --doctor     「목소리 모델 있나」 true
빈 창고 · fixtures --쓴다 시험표                 ✘ 0 · 90초 · 남긴 3·버린 5 → 휴지통 5 · 다시 세기 0.3초
                                             · AI 1단 283 · 한 편 625 · full=1 854 · 491MB
```

# 2. 할 것

## 가. ★ 본론 — 딴 작업 폴더에서 켜기
```
□ C:\VC\app 에 덮어 푼다 (방화벽 창은 이번에도 안 떠야 한다 — 뜨면 적어라)
□ 작업 폴더를 exe 옆이 아닌 곳으로 두고 켠다. 예:
    Start-Process C:\VC\app\VC.exe -WorkingDirectory C:\Windows\System32
  → 창 「VC」가 뜨나 · 그 작업 폴더에 data 폴더가 안 생겼나
□ v0.1.88 에서 네가 막혔던 그 방법(창 관찰 도구로 직접 띄우기)으로도 한 번
```

## 나. 목소리
```
□ VC.exe --doctor → vc-진단.json 의 「목소리 모델 있나」 가 true 인가
```

## 다. 시험표 — 기본 창고 하나만 (새 빈 창고 + fixtures 사본 --쓴다)
장문 창고는 이번엔 건너뛴다 — 이번 판은 그쪽을 안 건드렸다.

## 라. 따로 잰다 (1~2개, 네가 고른다) → 보고서를 올린다

## 마. 판단 몫 — **이번엔 없다.** 크레딧을 아낀다.

# 3. 보낼 것

`★잰-것-v0.1.89-<날짜>.md` — AGENTS.md 4장의 꼴대로. 마지막 줄에 쓴 토큰(못 보면 「못 쟀다」).
