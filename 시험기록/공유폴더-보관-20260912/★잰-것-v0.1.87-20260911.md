# ★잰-것-v0.1.87 — 시험 PC (Codex 크레딧이 떨어져 Claude 가 이어서 잼) · 2026-09-11

★ **시험 PC 에서 잰 것이다** (만든 PC 아님). 잰 이는 Codex 가 아니라 이 PC 의 Claude 세션이다.
만든 쪽과 다른 기계·다른 창고에서 쟀지만, 에이전트 종류가 만든 쪽과 같은 Claude 라서 「다른 방향」은 약하다.
다음 판부터는 다시 Codex 가 본다.

- 판 v0.1.87 · zip SHA-256 04BAD011… = `.sha256` [잰 것]
- 푼 자리 `C:\VC\app` (Codex 가 09-10 에 풂) · exe 옆 `models\qwen2.5-1.5b-instruct-q4.gguf` · `기록자리.txt` 있음 [잰 것]
- 창고: Codex 창고 두 개(`vc-store-0187` · `vc-store-long-0187`)를 `C:\VC\시험-claude\fix` · `long` 으로 **복사해서** 거기서 쟀다.
  원본 창고는 안 건드렸다. `기록자리.txt` 는 끝나고 원래 값(long 창고)으로 되돌렸다.

## 1. 시험표 요약 (Codex 가 09-10 23:52 에 올린 두 장)

✘ 0. 만든 PC 값과 견줌:

| 무엇 | 만든 PC | 시험 PC | |
|---|---|---|---|
| 흡수 세기 → 쓰기 | 남긴 3·버린 5 (38초) | 3·5 (26.8초) | 같다 (시간만 다름) |
| 다시 세기 | 후보 0·겹침 9 | 0·9 | 같다 |
| AI 1단 / 한 편 / full=1 | 283 / 625 / 854 | 283 / 625 / 854 | 같다 |
| 최소 창 크기 | 1078x430 | **1074x430** | 가로 4px 다르다 → 아래 2장에서 따로 잼 |
| 메모리 | 491MB | 495MB | 비슷 |
| 엔진 줄 | 모델 1개 대기 · 탈 없음 | 같다 | 같다 |

장문 창고: 1단 356 · 한 편 3226 · full=1 3457 = 안 바뀔 값. 「2단 한 토막」 3147 은 열린 문제 6 (도구 탓, 이미 가름).

## 2. 따로 잰 값 — 시험표를 안 거치는 법으로

| 무엇 | 표 | 따로 잰 법 | 잰 값 | |
|---|---|---|---|---|
| 최소 창 크기 | 1074x430 | Win32 `MoveWindow(200x150)` 뒤 `GetWindowRect` → 바깥 1090x469. 같은 창의 바깥−안 차이 16x39 (`GetClientRect` 1180x760 · 바깥 1196x799) | 안쪽 **1074x430** | 같다 [잰 것]. 만든 PC 1078 과의 4px 는 테두리·DPI 차이로 본다 [짐작이다] — 이 PC DPI 96 |
| 메모리 | 500MB | 켜고 40초 뒤 `Get-Process` | WorkingSet **498MB** (Private 653MB) | 같다 [잰 것]. 표 값은 WorkingSet 쪽으로 보인다 [짐작이다] |
| 한 토막 「저장과 중복 방지」 | (표는 딴 소제목) | Bearer 헤더로 서버에 직접 (`note?title=&heading=`) | **849** | 안 바뀔 값과 같다 [잰 것] |
| 한 토막 「입력과 경계 조건」 | 만든 PC 831 | 같은 법 | **831** | 같다 [잰 것] |
| 글 한 편 | 3226 | 같은 법 | **3226** | 같다 [잰 것] |
| 없는 소제목 | 404 · 162자 | 소제목 「없는 소제목」 | 404 · 159자 | 글자 수는 물은 소제목 이름 길이를 따르는 것으로 본다 [짐작이다] |
| 1단 search · full=1 | 356 · 3457 | q=「문서 도구 검증 지침」 | 360 · 3461 | **둘 다 +4.** 표가 쓴 물음말을 몰라서, 물음말 길이 차로 본다 [짐작이다] |

※ `full=1` 은 **search 쪽** 인자다. `note?title=…&full=1` 로 부르면 무시되고 글 한 편과 같은 3226 이 온다 [잰 것] — 손으로 재는 사람이 헷갈릴 자리.
※ q=「긴 글 검증 절차」 는 542 · full=1 3713 — Codex 가 VC.md 에 그 링크 줄을 넣어 둔 탓에 VC 가 결과에 걸려 늘었다 [잰 것]. 견주지 마라.

## 3. 판단 몫 — 진짜 창을 눌러서 (computer-use · 1회씩)

| 무엇 | 결과 | |
|---|---|---|
| 목차 → 「입력과 경계 조건」 | 맨 위로 온다 | ✔ [잰 것] |
| 목차 → 「저장과 중복 방지」 | 맨 위로 온다 | ✔ [잰 것] |
| 목차 → 「외부 변경과 복구 검증」(끝) | 글 끝이라 위에서 몇 줄 아래 — 만든 쪽과 같다 | ✔ [잰 것] |
| VC 글의 `[[긴 글 검증 절차]]` 누름 | 열린다 | ✔ [잰 것] |
| 「←」 단추 | VC 로 돌아온다 | ✔ [잰 것] |
| 링크 다시 누르고 `Alt+←` | 끝 화면이 VC | ✔ [잰 것 — 사이 화면은 안 봤다] |
| 「찾은 것」 줄 한 번 누름 | 카드가 열린다 (만든 쪽 「쓰다가 걸린 것」 3번 재현 안 됨) | ✔ [잰 것] |

### 열린 문제 7 — 이 자리에서 **재현 안 됨** (셋 다 1회)

```
a) 목차 콤보를 쓴 뒤 검색칸 누르고 침  → 검색칸에 들어감
   (단 앞 검색말이 안 지워지고 뒤에 붙음: 「긴 글 검증 절차초점시험」)
b) 「고치기」로 편집 중 검색칸 누르고 침 → 검색칸에 들어감 · 본문 파일 해시 그대로 [잰 것]
c) 읽는 칸 빈 곳 한 번 누름 → 고치기로 안 넘어감
   두 번 누름 → 낱말 「절차」만 골라짐, 고치기 안 넘어감 → 그 뒤 친 말도 검색칸 · 해시 그대로 [잰 것]
```

만든 쪽이 본 「빈 곳을 누르면 고치기로 넘어간다」가 이 판·이 자리에서는 안 났다.
**짧은 글(VC)에서만 쟀다** — 긴 글 안의 빈 곳은 못 눌러 봤다 [못 쟀다].
computer-use 누름은 사람 손보다 빠르고 정확하다 — 「누름이 빨랐나」는 여기서도 못 가른다.

### 열린 문제 5

- **밖에서 고친 것** — 창을 켜 둔 채 VC.md 끝에 한 줄 덧붙임(PowerShell, 02:24:21) →
  10초 뒤 열린 카드에 그 줄이 보임 ✔ · 창 검색 「밖에서 고친」 → VC 가 맨 위, 조각에 그 줄 ✔ ·
  API search 도 VC 를 냄 ✔ [잰 것 1회]. 몇 초 만에 보였는지는 [못 쟀다] (10초 뒤 한 번 봄).
- **창 검색이 휴지통 글을 안 내놓나** — fix 창고(휴지통 5)에서 휴지통 글에만 있는 「로컬 문서 백업」 →
  창 목록 4개 = 남긴 항목 전부, 휴지통 글 없음 ✔. API 「로컬 문서 백업」·「잡담 인사」 → 결과 0 ✔ [잰 것].
  ※ 곁가지: 창 검색은 **어느 글에도 없는 말에도** 「관련 4개야」라며 항목 4개를 다 내놓는다
  (「밖에서 고친」도 「관련 5개」). 항목이 4~5개뿐인 창고라서인지는 못 갈랐다 [짐작이다].
- **방화벽** — 방화벽 이벤트 로그로 잼 [잰 것]:
  ```
  09-10 23:51:59  C:\vc\app\vc.exe  Block 규칙 생김 (2097)
  09-10 23:52:03  같은 규칙 Allow 로 바뀜 (2099)
  ```
  = 처음 켤 때 방화벽 창이 떴고 4초 뒤 허용이 눌린 흔적이다 [짐작이다 — 누가 눌렀는지는 Codex 기록에 없다].
  오늘 02:13 · 02:25 두 번 같은 exe 로 켰을 때 vc.exe 방화벽 이벤트 **0** → 안 떴다 [잰 것].
  단 **같은 판**이다. 「다음 판을 같은 폴더에 덮어 풀면 안 뜨나」는 **다음 판에서만** 잴 수 있다.
  규칙 이름이 이번 판부터 「VC — 로컬 우선 개인 AI 작업공간」(옛 판은 「vc.exe」).
  옛 판 폴더마다 남은 규칙 17쌍(v0.1.77 · v0.1.80 은 Block) — 손대지 않았다.
- 30분 써 보기 — 지시서대로 이번엔 안 했다.

### 엔진 줄 — 열린 문제 3·4

두 창고 모두 「엔진 local · 모델 1개 대기」, 서버 전화 마지막 탈 「(없음)」 [잰 것 — 시험표와 내 화면].
창고의 `eb_config.json` model_dir 은 **옛 v0.1.82 자리**를 그대로 가리키는데도 모델을 찾는다 → 문제 3 이 이 자리에서도 닫힘 [잰 것].
「서버에 못 물어봤다」는 안 났다.

## 4. 못 잰 것 / 짐작인 것 / 곁에서 본 것

- **vc-죽음.log** — Codex 가 켰을 때 `Windows fatal exception: code 0x8001010d` 가 long 창고 6번 · fix 창고 1번.
  창은 모두 「끔 (0)」 정상 종료. 걸린 스레드는 `brain.py onnx_embedder` 세션 만들기 / `embed` 중, 주 스레드 `eb.py:355`.
  **내가 켠 두 번(02:13 · 02:25)엔 새 줄 없음** [잰 것].
  fix 창고의 한 줄(23:52:01)은 방화벽 창이 떠 있던 23:51:59~23:52:03 사이다 → 방화벽 창이 뜬 사이의 COM 호출로 보인다 [짐작이다].
  0x8001010d = RPC_E_CANTCALLOUT_ININPUTSYNCCALL. faulthandler 가 적은 비치명 예외일 수 있다 [짐작이다]. 몇 번째 켬부터 났는지는 로그에 시각이 없어 [못 쟀다].
- **Codex 가 멈춘 자리** — long 원본 창고 VC.md 에 「링크 이동 시험: [[긴 글 검증 절차]]」 줄을 넣은 채 안 되돌렸다
  (시험 전 모습은 `work\VC-링크시험전.md`). 원본은 그대로 두었다.
- 긴 글 안의 빈 곳 누르기 · Alt+← 사이 화면 — [못 쟀다].

쓴 토큰: 못 쟀다 (Claude 세션이라 Codex 식 값이 안 보인다. 남은 컨텍스트 표시로 짐작하면 약 16만 [짐작이다])
