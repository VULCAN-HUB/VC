# v0.1.88 — **덮어 풀 때 방화벽**이 본론이다

```
VC-v0.1.88.zip
SHA-256  96BB4330A04E396DECC737B0A9B40FF75AB8D8CCDCB2E249289C5D299A9E3BDC
```

AGENTS.md 차례대로 돈다. `현재상태.md` 를 먼저 읽어라 — 열린 문제 7 은 **VC 결함이 아니었다**(닫힘).

---

# 1. 새로 든 것

```
시험표  「2단 한 토막」이 둘째 소제목을 재고 **이름을 적는다** (v0.1.87 은 글 전체를 쟀다)
        재기 전에 색인을 파일에 맞춘다 — 파일만 넣은 창고도 AI 꺼내기를 건너뛰지 않는다
자국    읽는 칸에 친 말이 글로 들어갈 때 `vc-기록.log` 에 「읽다 쳐서 고치기로 — 초점 … · 최근 누름 …」 한 줄
```

## 만든 쪽에서 구운 판으로 잰 것

[잰 것, 만든 PC · 구운 v0.1.88 · gguf 는 exe 옆 models\] 두 창고 모두 ✘ 0.

```
장문 한 장 · 색인 없는 창고 (폴더 없이)   21초
  AI 1단 356 · 2단 한 토막 「입력과 경계 조건」 831 · 한 편 3226 · full=1 3457   ← 고침 둘 다 먹었다
빈 창고 · fixtures --쓴다                 93초
  흡수 남긴 3·버린 5 → 3개 씀·휴지통 5 → 다시 세기 후보 0·겹침 9 (0.3초)
  AI 1단 283 · 한 편 625 · full=1 854 · 화면 최소 1078x430 · 491MB · 엔진 「local · 모델 1개 대기」
```

★ 네 자리 값이 이것과 다르면 그게 보고 거리다. 같으면 「같다」 한 줄이면 된다.

# 2. 할 것

## 가. ★ 본론 — 같은 폴더에 덮어 풀고 처음 켤 때
```
□ v0.1.88 을 **v0.1.87 이 있던 그 폴더(C:\VC\app)에 덮어** 푼다 (models\ · 기록자리.txt 는 남는다)
□ 처음 켤 때 윈도 방화벽 창이 뜨나 — **누르지 말고** 떴는지만 적어라
□ 방화벽 이벤트 로그로도 본다 (네가 v0.1.87 에서 쓴 법 그대로 · 2097/2099 가 새로 생기나)
```
판마다 방화벽 창이 뜨면 오너가 판마다 눌러야 한다. 안 뜨면 그 손이 사라진다.

## 나. 시험표
```
□ 새 빈 창고 + fixtures-realistic 사본 --쓴다     ← 기본 창고
□ 네 long 창고에서 폴더 없이 --시험표             ← 「2단 한 토막」 줄이 「입력과 경계 조건」 831 인가
```

## 다. 따로 잰다 (1~2개, 네가 고른다) → ★ **여기까지 쓴 보고서를 먼저 올린다**

## 라. 판단 몫 — 하나만 · 스크린샷 다섯 장까지
```
□ 긴 글 목차 → [[링크]] → ← 한 바퀴 (새 판 회귀 확인)
```

# 3. 만든 쪽이 못 보는 자리 (네가 봐 주면 좋은 것)

만든 쪽은 이 네 자리를 **구조적으로 못 본다**. 시간이 남으면 하나라도:

- **방화벽** — 만든 PC 는 이미 허용해 둬서 창이 안 뜬다
- **한글 입력기로 치는 중(조합 중)** 의 동작 — 만든 쪽은 글자를 통째로 붙여 넣는다
- **해상도·배율 차이** — 최소 창이 만든 PC 1078 · 네 자리 1074 로 4px 갈렸다
- **Claude 앱 창이 화면을 덮는 자리** — 만든 PC 에선 화면 위쪽 오른편(검색칸 자리)의 누름을 Claude 창이 먹었다.
  네가 화면 조작 도구로 누를 때 안 먹으면 그 자리 맨 위 창이 누구인지부터 봐라

# 4. 보낼 것

`★잰-것-v0.1.88-<날짜>.md` — AGENTS.md 4장의 꼴대로. 마지막 줄에 쓴 토큰.
