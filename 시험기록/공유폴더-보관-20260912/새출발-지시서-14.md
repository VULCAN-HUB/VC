# 지시서 14 — **30분 써 봐라.** 새 판 없음 (열린 문제 5)

★ zip 이 없다. AGENTS.md 2장의 1)~3)(sha · 덮어 풀기 · 시험표)은 **건너뛴다.** 판은 v0.1.90 그대로.
이번엔 시험표가 없다. **이 판의 값은 숫자가 아니라 「쓰다가 걸린 자리」다.**

---

# 1. 할 것 — 사람처럼 30분

정해진 차례 없이 **실제로 쓰듯이** 써라. 한 창고를 골라(long 창고든 새 창고든) 그 안에서만 논다.

```
글을 새로 쓴다 · 고친다 · 제목을 바꾼다 · 지운다 · 되돌린다
찾는다(있는 말·없는 말·따옴표·tag: 같은 좁히는 말)
[[링크]] 를 걸고 눌러 간다 · ← → 로 오간다 · 목차로 뛴다 · 태그를 단다
그물(그래프)을 돌려 본다 · 곁에 띄운다 · 창을 좁혔다 넓힌다
밖에서(메모장) 고쳐 보고 창이 알아채나 본다
휴지통에서 하나 되살려 본다
```

## 적을 것 — 값은 여기다
```
□ 어디서 손이 멈췄나 · 무엇을 못 찾았나 · 무엇이 무서웠나(지워질까 봐 등)
□ 옵시디언에서는 되는데 여기서는 안 되는 것        ★ 제일 값진 보고다
□ 말이 어색하거나 거짓말하는 자리(화면 문구·말풍선)
□ 느리다고 느낀 자리 — 몇 초쯤인지 어림이라도
```
- 스크린샷은 **모양을 판단할 때만, 다섯 장까지.**
- 잰 값이 아니면 `[짐작이다]`, 안 해 봤으면 `[못 쟀다]` 로 적어라.

# 2. 끝나고 — 죽음 기록 (열린 문제 12)
```
□ 창을 정상으로 닫고, 그 창고의 vc-기록.log 마지막 줄에 「끔 (0)」 이 찍혔나
□ VC.exe --report → 「무엇이 어디.json」 의 「죽음 기록」 줄을 그대로 적어라
□ 30분 동안 자란 줄이 있으면, 그 줄과 아래 몇 줄을 옮겨 적어라 (집 폴더는 ~ 로)
```

# 3. 보낼 것

`★잰-것-지시서14-실사용-<날짜>.md` 한 장. **30분을 다 못 채워도 거기까지 쓴 것을 올려라** —
도중에 멈춰도 값진 것은 남는다. 올렸으면 다시 `테스트시작.txt` 를 기다린다.
