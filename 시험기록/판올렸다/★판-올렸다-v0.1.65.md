# v0.1.65 올렸다 — v0.1.64 가 안 뜨던 것을 고쳤다

```
VC-v0.1.65.zip
sha256  693E035896A631FEDC084FD291071E5B76663DA2DC2DAE96EB480CA1413703D0
지시서  딴PC-시험-지시서-27.md  (그대로 쓴다 — v0.1.64 에서 하나도 못 쟀으므로)
```

공유폴더 sha 재검 일치 · exe 0.1.65.0 확인.
★ **이번엔 exe 를 실제로 띄워 봤다** — 45초 뒤 살아 있고, 창 제목 「VC」, 862MB.

## 무엇이었나

`main()` 의 매개변수를 내 패치가 통째로 덮어썼다. 본문은 `argv` 를 쓰는데 받는
자리가 사라져 `UnboundLocalError`. `--doctor` 는 되고 인자 없이만 터진 갈래가 그래서 맞다.

★ **검사 열셋에 `eb.py` 가 아예 없었다.** 띄우는 길을 가진 파일이 목록 밖이라
서명이 사라졌는데 전부 초록불이었다. 이제 14개로 늘렸고, `main` 이 `argv` 를
받는지·기본값이 있는지·본문이 쓰는 이름과 맞는지 본다. 되돌리면 그 자리에서 터진다.

그리고 **굽고 나서 exe 를 실제로 띄우는 것**을 절차에 넣었다.
검사 통과가 「뜬다」는 뜻이 아니었다.

## 지시서

**27 번을 그대로 쓴다.** v0.1.64 에서 하나도 못 쟀으니 처음부터 재면 된다.
