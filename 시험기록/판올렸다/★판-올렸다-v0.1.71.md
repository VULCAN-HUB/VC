# v0.1.71 올렸다 — 모르는 스위치를 막았다

```
VC-v0.1.71.zip
sha256  A0F7479AE1BFC7A145A135A3A8C7AA79941162D238F0C0B45112ADE3F9D8659D
지시서  딴PC-시험-지시서-30.md
```

공유폴더 sha 재검 일치 · exe 0.1.71.0 · 띄워 봤다(창 제목 「VC」 · 483MB).
★ **실물 exe 에서 모르는 스위치도 확인했다** — 「모르는 것이다: --없는스위치」 찍고
종료값 2, **창 안 뜬다.**

## v0.1.69·v0.1.70 은 안 보낸다

그 사이 것이 다 여기 들어 있다. **안 뜨는 판을 한 번 보낸 뒤로 중간 판을 안 쌓는다.**

## 든 것 넷

1. **모르는 스위치 막이** — 시험 쪽이 잡았다. 없는 스위치를 조용히 무시해서,
   그대로 쟀으면 「제목 벡터를 빼도 안 좋아진다 = 고치지 마라」라는 **정반대의 거짓말**이
   나올 뻔했다. 이제 안 돌고 종료값 2.
2. **`--도움말`** — 명령줄로 물었으면 명령줄로 답한다(창 안 띄운다).
3. **`--제목벡터빼고`** — 뜻 검색에서 제목 벡터를 빼고 재 볼 수 있다. **기본은 안 바꿨다.**
4. **정답 여럿** — 물음 파일에 `물음 | 정답1; 정답2`.

## ㉮ 원인

    조사 어긋나감      본문 0.7894 · 제목 0.7900
    어색한 문장 고치기   본문 0.7856 · 제목 **0.8018**  ← 이 최댓값이 1등을 가져간다

본문끼리는 정답이 이기는데 **엉뚱한 글의 제목이 정답의 본문을 덮는다.**
지시서 30 「나」의 넷을 재서 정한다.
