# v0.1.67 올렸다 — 쓰던 색인이 깨지던 것을 고쳤다

```
VC-v0.1.67.zip
sha256  66885E46CCDFF45BEE934FA92876A6469BC044FA18D34A9112DD6DFD3F0A7746
지시서  딴PC-시험-지시서-28.md  (그대로)
```

공유폴더 sha 재검 일치 · exe 0.1.67.0 · 띄워 봤다(창 제목 「VC」 · 863MB).

## 무엇이었나

`links` 표에 `흐림` 칸을 더했는데 `CREATE TABLE IF NOT EXISTS` 는 **이미 있는 표를
안 고친다.** 새 색인에만 칸이 생기고 쓰던 색인에는 안 생겨 새 글 쓰기가 통째로 죽었다.
만든 쪽은 기록을 새로 부어서(빈 색인) 한 번도 못 봤다 —
**새로 만드는 길만 재고 올려 쓰는 길을 안 밟았다.**

## ★ 실물로 확인했다

붓기 전 사본(옛 색인 · 3142장 전부 0판)을 그대로 열어 봤다:

    항목 수 3142 · 판 적합률 0/3142 (0.0%)가 1판
    연결 수 0 · 흐린 선 0 · **색인 읽기 실패 없음**

칸이 붙고, 읽기가 성하고, **판 적합률은 0% 그대로**다 — 칸만 붙지 항목은 안 바뀐다.

## 고친 것 셋

- `wrote`(notes 표)·`흐림`(links 표) 을 **쓰던 색인에도 붙인다**
- **올려 쓰는 길을 검사에 넣었다** — 옛 스키마 색인을 일부러 만들어 놓고 새 글을 쓴다
- `--doctor` 에도 판 적합률·연결 수·흐린 선

## 고치다 하나 더

칸 붙이기를 매번 던지고 받는 쪽으로 했더니 검사에서 죽는 일이 **1/5 → 4/5** 로 늘었다.
다섯 번씩 재서 갈랐고, 있는지 먼저 보고 없을 때만 붙이니 **5/5**(원래 4/5 보다 낫다).
