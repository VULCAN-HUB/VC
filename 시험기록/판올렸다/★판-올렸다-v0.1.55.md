# v0.1.55 올렸다

```
VC-v0.1.55.zip
sha256  1BA562B9E5A3F0EFF79B71D46704CB72D969DD392499C7B399BDB75AF96413A2
지시서  딴PC-시험-지시서-18.md
```

공유 폴더에 올린 뒤 그 자리에서 sha 를 다시 재서 일치를 확인했다.

## 이번 판

1. **메모리가 스스로 말한다** — 진단에 메모리와 「켠 지」가 같이 찍히고,
   30초가 안 지났으면 「아직 다 안 올라왔다」가 붙는다. 네 24-1 의 규칙을
   물건 쪽에 넣은 것이다. 이제 밖에서 따로 잴 일이 없다.
2. **판 번호가 exe 와 진단에 들어갔다** — 그동안 exe 속성은 `0.1.0.0` 이었다.
   이제 `0.1.55.0` 이고 진단 맨 위에도 `판: v0.1.55` 가 찍힌다.
3. 기록 자리를 쪽지로 옮기는 길 — **네 PC 는 안 건드렸다.** 예전 그대로다.
4. 흡수 쪽 문지기·제목 고침 (네가 흡수를 안 돌리면 안 보인다).

## 이번에 꼭 재 줄 것

**메모리를 뜬 직후와 30초 뒤에 각각.** 「다시 봐라」가 붙었다 사라지는지 본다.
**0MB 가 찍히면 그것만으로 보고할 값이 있다.**

그리고 네 24-2 대로 **마지막으로 잰 판이 v0.1.52** 라, CPU·창 크기·상태줄·⚠
를 한 번 훑어 달라. 세 판이 비어 있다.

## 판 쌓아 둘 필요 없다

오너가 정했다. 개발 쪽 노트북에 소스 이력이 생겼고, 되돌릴 일이 있으면
거기서 그 판을 다시 구워 보낸다. **공유 폴더에는 지금 판 하나면 된다.**
v0.1.52 · v0.1.53 zip 은 지워도 된다 — 네가 판단해라.
