# 오너 결정이 필요한 것 — 한 곳에 모음

오너 지시(2026-09-13): 선택이 필요한 것은 여기 모아 두고 다른 것부터 진행한다. 묻는 일은 뒤로 미룬다.
각 줄: 무엇 · 왜 필요한가 · 내 추천(짧게).

| # | 무엇 | 왜 | 추천 |
|---|---|---|---|
| 1 | 바깥 AI API 키 보관 방식 (2번 기둥) | ~~지금 `eb_config.json` 평문~~ → **추천대로 냈다(v0.5.5 묶음)**: `keystore.py` 윈도우 자격 증명 관리자 · 맥 키체인. 켤 때 평문 키를 옮기고 설정에서 지운다. 보관소 없는 OS 는 평문 그대로 | 바꾸려면 말해 달라 |
| 2 | 제공자 고르는 화면을 언제 만드나 | ~~모델 고르개는 로컬 모델만 다룬다~~ → **추천대로 냈다(v0.5.12 묶음)**: 1번 기둥의 락·말없는 실패·개인정보 훑기를 마친 뒤, 설정 창 「바깥 AI」(종류·주소·모델·키→보관소, 다시 켜면 적용) | 값·한도 재기는 아직 — 바꾸려면 말해 달라 |
| 3 | 바깥 플러그인 문 열기 기준 | ~~지금은 부르는 길 없음~~ → **추천대로 냈다(v0.5.5 묶음)**: `POST /eb/v1/skills/propose` 선언문만 · 제안 줄 · 사람 승인 · 있는 모듈만 · 원격 403 | 바꾸려면 말해 달라 — 코드 실행 문은 여전히 닫혀 있다 |
> ★ **2026-09-13 오너: 「추천하는 대로」 — 4~10번을 모두 추천대로 확정.** 아래 「추천」 칸이 곧 결정이다.

| 4 | 시험 PC(Codex) 고리 다시 켤 때 | 2026-09-12 부터 멈춤 | **확정**: 굵은 판(0.6) 나올 때 한 번 |
| 5 | 큰 뜻 모델(e5-base 296MB)을 설치본에 기본으로 넣나 | 찾기 10→12/20, 대신 설치본 커짐 | **확정**: 넣지 않고 첫 실행 때 받기 권유(지금 방식 유지) |
| 6 | 창고 규칙 1조(링크는 선택) 유지 여부 | 창고 95% 가 링크 없음 — 뜻 검색으로는 찾힘 | **확정**: 유지 |
| 7 | 깨진 설정 복구 시 새 열쇠 발급 → 폰 다시 짝짓기 | 자동 복구의 대가 | **확정**: 유지(안 켜지는 것보다 낫다) |
| 8 | Google 계정 연결 방식 | 오너 요청(외부 계정 연결). 구글은 토큰 붙여 넣기가 아니라 **OAuth 로그인 창** — Google Cloud 에 앱(클라이언트 ID)을 오너 계정으로 등록해야 한다. 등록 없이는 못 붙인다 | **확정 → 냈다(v0.5.17 묶음)**: `google_auth.py` 설치형 OAuth(루프백 127.0.0.1 · PKCE · state 대조) · 읽기 전용 범위(drive.readonly · calendar.readonly) · 새로고침 토큰·클라이언트 비밀은 보관소에만 · 설정 「외부 연결」 에 ID·비밀 칸과 「구글 로그인」 단추 · `connect/google?path=/drive/v3/files` 로 읽기. **남은 오너 일: Google Cloud 콘솔에서 「데스크톱 앱」 OAuth 클라이언트를 만들어 ID·비밀을 넣고 로그인** |
| 10 | 옵시디언에 있고 VC 에 없는 남은 것 — 캔버스 · 수식(LaTeX) · mermaid 도표 · PDF 내보내기 · 여러 창고 전환 | 「옵시디언 상위호환」 잣대로는 남은 틈이다. [잰 것] 실무 창고 2,820장에 수식 1장 · mermaid 0장 · 그림 1장. 수식·mermaid 를 그리려면 웹 엔진을 넣어야 해 설치본이 수백 MB 커진다 | **확정: 안 만든다**(쓰임이 0~1장). 여러 창고 전환은 `기록자리.txt` 로 이미 자리를 바꿀 수 있으니 화면 단추만 필요할 때. 만들라고 하면 그때 한다 |
| 9 | VC 가 창고 대화에서 **스스로** 오너 정보를 뽑을까 | 지금은 VC 를 쓰는 AI 가 `me/learn` 문으로 적는다. 스스로 뽑으려면 로컬 모델이 대화 로그를 읽는다(개인정보·틀린 짐작 위험) | **확정 → 냈다(v0.5.17 묶음)**: `selflearn.py` — 로컬 대화 모델이 있을 때만 주기 분석 뒤 새 대화의 오너 말만 읽어 짐작으로(질문 목록 안 · 한 말에 있는 낱말만 · 한 번에 5개), 이 PC 밖으로 안 나간다 |
