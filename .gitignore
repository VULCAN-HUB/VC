# 소스만 남긴다. 아래는 다시 만들 수 있거나, 남기면 안 되는 것들이다.

# 기록 — 오너 개인 내용이다. 절대 안 올린다.
pc/data/

# 모델 4.1GB · 빌드 재료 28MB — 받으면 되는 것들이다.
models/
빌드전용/

# 만들어지는 것 — 소스에서 다시 나온다.
pc/dist/
pc/build/
__pycache__/
*.pyc
*.zip
