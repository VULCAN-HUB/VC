# 행성 — 기계마다 제 창고, 닿으면 서로 본다

지금은 딴 PC 가 붙으면 기록이 **한 덩어리로 합쳐진다.** 그래서 그래프만 봐서는
이 글이 맥에서 쓴 건지 윈도우에서 쓴 건지 알 수 없다.

바꾸려는 것: **기계마다 제 로고(행성)를 갖고 제 기록만 거느린다.** 로고끼리만 잇는다.

> 정한 날 2026-09-26 · 오너와 하나씩 물어 가며 정했다. 아직 **안 만들었다** —
> 이 문서는 무엇을 만들지 적어 둔 것이다.

## 정한 것

| 무엇 | 어떻게 |
|---|---|
| 쌓이는 자리 | **기계마다 따로.** 맥에서 쓴 건 맥에 |
| 남의 기록 | **사본을 받아 둔다.** 꺼져 있어도 읽힌다 |
| 고치기 | 늘 **주인 기계**로 보낸다. 주인이 꺼져 있으면 **읽기만** |
| 맞추는 때 | **실시간** — 바뀌는 즉시 |
| 찾기 | 전부 내 기계에서 (사본이 있으니 즉시 뜬다) |
| 새 글 | **내 행성**에 쌓인다 |
| 이름 겹침 | 앞에 행성 이름 (`맥 1/회의록`) |
| 선의 뜻 | 「지금 닿을 수 있다」 |
| 선이 많아지면 | 흐리게 깔고 **손 댄 행성에 닿는 선만** 밝힌다 |
| 행성 넘는 링크 | 선은 안 긋고, **누르면 건너간다** |
| 꺼진 기계 | 회색 행성. 사본은 읽히고 고치기는 막힌다 |
| `기록자리.txt` | **없앤다** — 닿으면 자료가 이 PC 안에 있다 |

## 왜 사본을 받아도 괜찮은가

베끼기가 어려운 까닭은 보통 **양쪽이 다 쓰기** 때문이다. 여기는 **글마다 주인이 하나**다.
그래서 규칙 둘이면 충돌이 **구조적으로** 안 난다:

1. 받아 둔 것은 **읽기용**. 고치면 주인 기계로 보낸다
2. 주인이 꺼져 있으면 **읽기만** 된다

한 글을 쓰는 쪽이 늘 하나뿐이라 「어느 쪽이 참이냐」를 정할 일이 안 생긴다.

★ 대신 **지운 글이 유령으로 남는다.** 주인에서 지웠는데 사본에 남으면, 손님 PC 에
남은 글이 다시 넘어오던 그 병이 그대로 온다. 갱신할 때 **없어진 것도 지운다.**

## 이미 있는 것 (새로 안 만들어도 된다)

| | |
|---|---|
| `GET /eb/v1/memory/search` · `/memory/note` | 남의 창고 찾기·읽기 |
| `POST /eb/v1/memory` · `/rename` · `/delete` · `/trash/restore` | 쓰기·고치기·지우기 |
| `POST /eb/v1/wiki/ask` | 그 창고에 묻기 |
| `notes.알림걸이` → `/eb/v1/events` → `mesh.그물` | **바뀌면 바로 들린다** |
| 페어링 열쇠 · 테일스케일 | 서로 찾고 문을 연다 |

실시간의 뼈대가 이미 서 있다. 덧붙일 것은 **「들으면 그 글 하나만 받아 사본을 고친다」** 뿐이다.

## 만들 것

1. **사본 창고** — 행성마다 따로. 본문만 받고 붙임(사진)은 열 때 받는다
2. **들으면 받는다** — `/eb/v1/events` 를 듣고 바뀐 글만. 없어진 것도 지운다
3. **붙는 순간 따라잡기** — 끊겨 있던 동안 바뀐 것만
4. **고치기는 주인에게** — 사본을 고치지 않고 주인 기계로 보낸다. 꺼졌으면 막는다
5. **화면** — 행성으로 나눠 그리기 · 선 흐리게/밝히기 · 회색 행성
6. `기록자리.txt` 걷어내기

## 다 그릴 수 있나 — 해 보고 재 봤다 (2026-09-26)

**처음 적었던 「한 붓으로 몰아 그리면 11ms」는 틀렸다.** 그 값은 동그라미만 그려 본
것이라 선·바탕·이름표·투영이 빠져 있었다. 실제로 고쳐 놓고 잰 값:

| 2800장 | |
|---|---|
| 처음 | 320ms (3.1 프레임/초) |
| 고친 뒤 | **179ms** (5.6 프레임/초) |

1.8배지 30배가 아니다.

### 발목은 세 군데였다 — 하나씩 꺼서 갈랐다

| | 처음 | 지금 |
|---|---|---|
| 마디마다 Qt 항목 | 아무것도 안 그려도 342ms | **없앴다**(마디는 평범한 물건, `마디떼` 가 몰아 그린다) |
| **선** — 하나씩 긋고 줄마다 함수를 새로 만듦 | 138ms | 짙기끼리 길에 묶어 71ms |
| 마디 칠하기 | — | 68ms |

**제일 무거운 것은 선이었다.** 마디만 보고 있었는데 아니었다.
마지막 68ms 는 붓을 미리 만들어 둬도 안 줄었다 — **파이썬에서 `QPainter` 를
마디마다 두 번씩 부르는 값** 자체다. 거기서 멈췄다.

### 지금 쓸 수 있는 것

| 장 | 돌릴 때 |
|---|---|
| 400 | 34ms (30 프레임/초) — 전에는 83ms(12) 였다 |
| **700** ← 새 한도 | 70ms (14 프레임/초) — **전의 400장과 같은 느낌** |
| 2800 | 179ms (5.6) — 돌릴 때 끊긴다. 멈춰 있으면 멀쩡하다 |

한도를 400 → **700** 으로 올렸다. 전보다 나빠지지 않으면서 1.75배를 보여 준다.

### 「다 보이게」로 가려면

마디와 선을 **파이썬에서 하나씩 부르지 않는** 길이 필요하다. 한 번에 넘기는 그리기
(점 배열을 통째로 넘기거나, 여러 판을 미리 그려 두고 돌려 쓰기)를 재 봐야 한다.
자리 잡는 셈도 2000장에 235ms 라 처음 펼칠 때 오래 걸린다 — 둘 다 아직 안 풀었다.

## 안 정한 것

- 처음 붙을 때 사본을 **어떻게 받나** — 통째로 한 번에? 뒤에서 천천히?
- 사본이 **얼마나 오래된 것인지** 어떻게 보여 주나
- 행성을 **어떻게 배치하나** — 사람이 끌어다 놓나, 저절로 자리 잡나
