# 윈도우에 VC 깔기

이 문서 하나면 된다. **위에서부터 그대로** 따라 하면 된다.
(클로드 코드에게 「이 문서대로 해라」고 시켜도 된다.)

> 맥·윈도우 **둘 다** 쓰려면 마지막 5·6번까지 본다. 창고 하나를 함께 보고,
> 한쪽에서 고치면 다른 쪽이 바로 안다.

---

## 0. 먼저 — 여기서 하지 마라

**`C:\WINDOWS\system32` 에서 시작하면 안 된다.** 윈도우가 쓰기를 막아 놔서
`git clone` 이 「Permission denied」로 끝난다. PowerShell 을 **관리자로 열면**
거기서 시작한다 — 관리자로 열 필요 없다.

작업 자리로 옮긴다:

```powershell
cd D:\프로젝트          # 없으면:  mkdir D:\프로젝트; cd D:\프로젝트
```

## 1. 받기

```powershell
gh repo clone VULCAN-HUB/VC
cd VC\pc
```

`gh` 가 없으면 `winget install GitHub.cli` 뒤 `gh auth login`.

## 2. VC 전용 파이썬 자리 만들기

★★ **이미 있는 다른 venv 를 쓰지 마라.** 실제로 그렇게 하다 막혔다 —
`python` 이 딴 도구의 venv 를 가리키고 있었고, 거기 onnxruntime 이 깨져
`DLL 초기화 루틴을 실행할 수 없습니다` 로 끝났다. 남의 자리를 고치려 들면
그 도구가 망가진다. **VC 자리는 따로 판다**(맥도 `.venv-mac` 으로 그렇게 쓴다).

```powershell
$뿌리 = python -c "import sys; print(sys.base_prefix)"
& "$뿌리\python.exe" -V                      # 3.11 이상이어야 한다
& "$뿌리\python.exe" -m venv .venv
.\.venv\Scripts\python.exe -m pip install -U pip
```

★★ **`Activate.ps1` 을 쓰지 않는다.** 실행 정책이 막으면 거기서 끝난다
(실제로 「이 시스템에서 스크립트를 실행할 수 없으므로」로 멈췄다). 정책은 보안
설정이라 그걸 풀라고 하지 않는다 — **venv 의 파이썬을 경로로 곧장 부르면**
정책을 아예 안 건드리고, 새 창을 열어도 다시 할 것이 없다.

**이 다음부터 `python` 대신 `.\.venv\Scripts\python.exe` 로 적는다.**

- `py -3.11` 은 안 쓴다. uv 등으로 깔린 파이썬은 `py` 가 못 찾는다
  (「No suitable Python runtime found」). 위 `$뿌리` 는 지금 도는 파이썬의
  **뿌리 자리**라 그런 경우에도 잡힌다.
- 뿌리가 3.11 보다 낮으면 그때 깐다: `winget install Python.Python.3.12`

## 3. 딸린 것 깔기

```powershell
.\.venv\Scripts\python.exe -m pip install --only-binary=llama-cpp-python `
    --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu `
    pyinstaller PyQt5 onnxruntime tokenizers llama-cpp-python `
    faster-whisper sounddevice segno piper-tts
.\.venv\Scripts\python.exe -c "import onnxruntime, llama_cpp, tokenizers, PyQt5; print('준비됨')"
```

★ 확인 줄은 **onnxruntime 을 PyQt5 보다 먼저** 부른다. 거꾸로 하면 PyQt5 가 딸고 온
2019년 msvcp140 을 onnxruntime 이 물고 소리 없이 죽는다(access violation) — 앱은
`paths.py` 가 막아 두지만 이 한 줄은 그걸 안 거친다.

★ **`requirements-mac.txt` 를 쓰지 마라** — 맥 Metal 판이 섞여 있다. 위 목록이 윈도우용이다.

| 걸리면 | 이렇게 |
|---|---|
| onnxruntime 이 **DLL 오류** | `winget install --id Microsoft.VCRedist.2015+.x64` 뒤 다시 |
| `llama-cpp-python` 이 몇 십 분 컴파일하거나 터진다 | 위 줄의 `--only-binary`·`--extra-index-url` 을 빼먹었다 — 미리 구운 CPU 판을 받게 하는 두 개다 |
| `py` 가 없다 | 파이썬 3.11+ 를 먼저 깐다 (`winget install Python.Python.3.11`) |

## 4. 굽기 — 단일 설치 파일까지

설치 파일(`VC-설치-0.5.21.exe`)을 내려면 Inno Setup 이 있어야 한다. 한 번만 깔면 된다:

```powershell
winget install JRSoftware.InnoSetup
```

그다음:

```powershell
.\build.ps1
```

**자체점검 56개를 먼저 돌리고, 통과해야 굽는다.** 안 통과하면 안 구워진다 —
「윈도우에서만 나는 탈」이 설치본에 실리지 않게 하려는 것이다.

나오는 것 (`D:\프로젝트\_빌드파일\`):

| 파일 | 무엇 |
|---|---|
| `VC-설치-0.5.21.exe` | **단일 설치 파일.** 누르면 깔린다 |
| `VC.zip` | 풀어서 바로 쓰는 판(설치 없이) |
| `*.sha256` | 무결성 확인용이자 **백신 오탐 신고 근거** |

- 스크립트가 막히면: `powershell -ExecutionPolicy Bypass -File .\build.ps1`
- 자리를 바꾸려면: `.\build.ps1 -OutDir 어디`
- Inno Setup 이 없으면 **건너뛰고 zip 만** 낸다 — 굽기를 막지 않는다.
- 첫 실행에 **방화벽이 묻는다** — 허용해야 폰·딴 PC 가 붙는다.

### 설치 파일이 하는 것

- 관리자 권한을 **안 쓴다** — 회사 PC 에서도 막히지 않는다
- 돌고 있는 VC 를 **먼저 닫는다** — 반쯤 깔리는 것을 막는다
- 지울 때 **기록은 안 지운다** — 20년 치를 지우는 단추를 만들지 않는다

★ `onefile`(exe 한 개로 다 밀어넣기)로는 안 만든다. 재 봤더니 기동이
**3.95초 → 1.36초로 2.9배** 느려지고, 제 몸을 임시폴더에 푸는 것이 **백신 오탐의
대표 트리거**다. 폴더로 굽고 그것을 설치 프로그램으로 감싸는 쪽이 낫다.

## 4-1. 업데이트 — **VC 가 알아서 알려 준다**

켜고 잠시 뒤 VC 가 깃허브에 새 판이 있는지 본다. 있으면 묻는다:

> 새 판 v0.5.21 이 나왔어. 받아서 깔까?
> 기록·설정·모델은 그대로 남아 — 프로그램만 갈린다.

«받아서 깔기» 를 누르면 받아서 **셈(sha256)을 맞춰 보고** 설치 프로그램을 띄운다.
셈이 안 맞으면 지우고 안 깐다 — 받다 끊긴 것을 실행하는 것이 가장 나쁘다.

★ **조용히 갈아 끼우지 않는다.** 모르는 새 프로그램이 바뀌면 어제 되던 것이
  오늘 안 될 때 무엇이 바뀌었는지 알 길이 없다. 말하고 사람이 누른다.
★ 인터넷이 없거나 깃허브가 느려도 **켜는 것을 막지 않는다.**

### 새 판을 내는 쪽(개발 PC)에서는

```powershell
cd D:\프로젝트\VC ; git pull ; cd pc ; .\build.ps1
gh release create v0.5.21 D:\프로젝트\_빌드파일\VC-설치-0.5.21.exe `
                          D:\프로젝트\_빌드파일\VC-설치-0.5.21.exe.sha256
```

맥 쪽 `.dmg` 도 같은 릴리스에 올리면, 기계마다 **제 것을 골라** 받는다.

## 4-2. 손으로 다시 깔기

```powershell
cd D:\프로젝트\VC
git pull
cd pc
.\build.ps1
```

나온 `VC-설치-<판>.exe` 를 **그냥 덮어 깔면 된다.**

★★ **쓰던 것은 그대로 남는다.** 기록·설정·모델이 **앱 폴더 밖**에 살기 때문이다:

| 무엇 | 어디 | 덮어 깔면 |
|---|---|---|
| 창고(기록) | 문서 폴더 또는 `기록자리.txt` 가 가리키는 곳 | 그대로 |
| 설정·열쇠·색인 | `%LOCALAPPDATA%\VC` | 그대로 |
| 모델 | 모델 자리 | 그대로 |
| 프로그램 | `%LOCALAPPDATA%\Programs\VC` | 갈린다 |

앱 폴더 안에는 자료가 **하나도 없다**(실기로 확인했다).

## 5. AI 모델 — **VC 안에서 받는다**

저장소에는 모델이 없다. **처음 켜면 VC 가 말해 주고**, 오른쪽 «모델» 칸에서
«받기» 를 누르면 거기서 바로 받는다.

| 갈래 | 고를 수 있는 것 |
|---|---|
| 글자(대화) | Qwen2.5 1.5B · Qwen3 4B · Qwen3 8B |
| 사진 | Qwen2.5-VL 3B · 7B |
| 뜻 검색 | 큰 모델 |
| 목소리 | 한국어 (kss) |

주소는 코드에 든 목록에서만 온다 — 아무 데서나 받지 않는다. 받다 끊기면
목록에 안 뜨고, 언제든 멈출 수 있다.

## 6. 창고를 한 곳에 두고 여러 기계로 (선택)

**기록은 저장소에 없다.** 빈 창고로 시작해 스스로 채운다.
맥·윈도우가 **같은 창고**를 보게 하려면 기계마다 쪽지 한 장:

| 기계 | 쪽지 자리 | 안에 적을 것 |
|---|---|---|
| 윈도우 | `VC.exe` 옆에 `기록자리.txt` | `Z:\VC` (그 기계에서 보이는 경로) |
| 맥 | `~/Library/Application Support/VC/기록자리.txt` | `/Volumes/NAS/VC` |

★ 색인 db 는 **기계마다 따로** 만들어진다 — 네트워크 위에서 SQLite 를 쓰지 않는다.
★ 못 쓰는 자리를 가리키면 **조용히 기본 자리로 켜지지 않고** 그렇다고 말해 준다.

### 기기끼리 신호

저장·지움을 누르면 붙어 있는 다른 기기가 **바로** 안다(훑기를 기다리지 않는다).

1. 테일스케일을 양쪽 다 켠다
2. 설정 → **기기끼리 신호** → 「새 열쇠 만들기」
3. 나온 값을 **모든 기계에 똑같이** 넣는다 — 영문·숫자만
   (HTTP 머리말은 latin-1 이라 한글이 섞이면 붙을 때 깨진다)

주소는 안 적는다 — 테일스케일로 서로 찾는다.

## 7. 바깥 AI 에게 시키기 (선택)

VC 채팅에서 엔진을 `claude` 나 `codex` 로 바꾸면, 그 CLI 가 **VC 의 창고와
프로젝트 코드를 도구로 잡고** 일한다.

```powershell
npm install -g @anthropic-ai/claude-code    # 또는 codex
```

깔고 새 터미널에서 VC 를 켜면 알아본다.

---

## 확인하는 법

```powershell
.\.venv\Scripts\python.exe eb.py --모두검사   # 57개. 다 통과해야 정상
.\.venv\Scripts\python.exe eb.py --doctor     # 어디가 어떻게 잡혀 있는지 한 장으로
```

무엇이든 걸리면 **그 줄을 그대로** 들고 오면 된다.
