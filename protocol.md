# EB 프로토콜 v0.1 (폰 ↔ PC)

폰이 오케스트레이터 본체이고 PC는 서버다(결정 25). 따라서 **폰이 클라이언트, PC가 서버**다.
PC가 꺼져 있어도 폰은 동작해야 하므로, 모든 호출은 실패해도 이비가 죽지 않는 것을 전제로 한다.

## 설계 원칙

1. **OS 중립**(결정 20). HTTP/1.1 + JSON. 안드로이드 전용 IPC나 애플 전용 프레임워크를 쓰지 않는다.
2. **PC는 하나의 백엔드처럼 보인다**(결정 25). 추론은 **OpenAI 호환 규격**을 그대로 쓰므로
   폰 입장에서 내 PC와 클로드·제미나이는 같은 모양이다. 어댑터가 늘지 않는다(결정 3).
3. **EB 고유 기능만 별도 경로**를 쓴다. 학습 루프·기억·스킬 승인이 여기 해당한다.
4. **전송은 Tailscale 위**(결정 16). 프로토콜 자체는 전송을 가정하지 않는다.

## 인증

페어링 시 QR로 교환한 토큰을 헤더에 싣는다.

### 같은 PC 의 AI 가 붙는 법

이 창고의 마지막 쓸모는 **AI 가 꺼내 쓰는 것**이다. 그 AI 는 대개 같은 PC 에서 돈다.

1. VC 를 띄운다(창이든 `VC.exe --no-ui` 든 서버는 같다).
2. 열쇠는 **기록 자리의 `eb_config.json`** 안 `pair_token` 이다.
   자리를 모르면 `VC.exe --doctor` 가 「AI 가 붙는 법」 한 줄로 적어 준다.
3. 첫 부름은 `GET http://127.0.0.1:8765/eb/v1/hello` — **나머지 쓰는 법은 그 답이 다 준다**
   (항목 수 · 갈래별 수 · 태그 · 좁히는 문법 · 값이 얼마나 드는지).

```
curl -H "Authorization: Bearer <pair_token>" http://127.0.0.1:8765/eb/v1/hello
```

토큰 **값**은 진단 파일에 안 적는다 — 열쇠를 두 군데 두지 않는다. 자리만 말한다.

```
Authorization: Bearer <pair_token>
```

토큰은 페어링 1회로 생성되며 폰·PC 양쪽에만 저장된다. 계정 서버는 없다(결정 16).

## 엔드포인트

### 추론 — OpenAI 호환

```
POST /v1/chat/completions
```

규격을 새로 만들지 않는다. PC는 로컬 엔진(외부 엔진 연결, 결정 27) 앞의 얇은 창구다.

### 핸드셰이크

```
GET /eb/v1/hello
```

```json
{
  "protocol": "0.1",
  "models": ["qwen2.5-vl-7b", "llama3.1-8b"],
  "capabilities": ["inference", "log", "memory", "skills"],
  "busy": false
}
```

폰은 이 응답으로 **PC를 2단으로 쓸 수 있는지** 판단한다. 실패하면 3단(API)으로 내려간다.

### 학습 로그 — 지시 시작 시점부터 전송

```
POST /eb/v1/log
```

결정 17에 따라 **지시가 시작된 순간부터** 흘려보낸다. 배치가 아니라 이벤트 단위다.
PC가 꺼져 있으면 폰이 버퍼에 쌓아 두었다가 다음 연결 때 보낸다.

```json
{
  "instruction_id": "01J...",
  "seq": 3,
  "ts": "2026-08-05T12:34:56.789Z",
  "phase": "module_run",
  "tier": "pc",
  "module": "product_search",
  "step": "vision_query",
  "latency_ms": 820,
  "tokens_in": 1150,
  "tokens_out": 96,
  "cost_krw": 0,
  "retries": 0,
  "clarify_count": 0,
  "outcome": "success",
  "failure_point": null,
  "detail": {}
}
```

**계측 필드는 처음부터 들어간다**(결정 17). 성공 사례를 최적화하려면 비교할 숫자가 있어야 하고,
계측은 나중에 못 붙인다.

- `phase`: `instruction_start` · `intent` · `module_select` · `module_run` · `render` · `done`
- `tier`: `phone` · `pc` · `api`
- `outcome`: `success` · `failure` · `clarified` · `cancelled`
- `failure_point`: 실패한 단계 이름. 성공이면 `null`
- 개입 반응(결정 23)은 `phase: "intervention"` + `outcome`에 `accepted`/`ignored`/`blocked`로 기록

미디어는 기본으로 보내지 않는다. **실패 원인 규명에 필요할 때만 프레임 1장**을 첨부하며,
그 프레임은 이미 얼굴 모자이크를 거친 것이다(결정 2).

### 기억 — 항목(노트)

```
GET  /eb/v1/hello                              창고가 무엇을 아는지 · 쓰는 법
GET  /eb/v1/memory/search?q=<질의>&k=5[&brief=1][&full=1]
GET  /eb/v1/memory/note?title=<제목>[&heading=<소제목>][&q=<물음>]
POST /eb/v1/memory       { "title": "카페 단골", "text": "...", "kind": "preference", "pinned": true }
GET  /eb/v1/graph
```

★★ **꺼내기는 두 단이다.** 몸을 통째로 주면 여덟 장에 46,000자(≈18,000토큰)가 나가는데
읽는 것은 몇 줄이다. **크레딧을 줄이는 것이 성능이다**(오너 지시 2026-09-12).

- **1단 `search`** — 제목 · 물음에 맞춘 한 줄 요약 · 글자 수. **몸은 안 온다.**
  빈 칸과 기본값은 안 싣는다(없는 칸은 「없다」로 읽는다). `k` 기본은 **5**다 —
  오너 창고(2794장)에서 6~8등에 정답이 하나도 없었고 다섯으로 줄여 37%를 아꼈다.
  - `brief=1` **제목만.** 「무슨 결정들이 있었나」처럼 목록을 훑을 때. 세 배 싸다.
  - `full=1` 몸까지. 예전 쓰임 호환용이라 새로 쓰는 쪽은 안 쓴다.
  - 좁히기: `q` 에 `kind:결정` · `tag:이름` 을 섞는다. `"따옴표"` 는 그 구절 그대로.
    낱말은 기본이 **모두 든 것**이고, `A OR B`(한글 `또는`)는 **하나라도 든 것**이다.
    **빼기도 된다** — `-kind:일`. 오너 창고는 대화 조각(`kind: 일`)이 2373/2794 장이라
    그것만 빼도 찾은 물음이 12 → 13 이었다(글자는 거의 같다).
    `year:2026` · `path:2026/09` 도 된다(경로는 `/` 로 적어도 윈도우에서 걸린다).
    `title:회의록`(옵시디언 `file:`)은 **제목만** 본다 — 본문에 그 말이 여러 번 나오는 글이
    제목이 그 말인 글을 덮을 때 쓴다.
    ※ **처음부터 아무 갈래로나 좁히지 마라** — 틀린 갈래로 좁히면 크게 잃는다(재 본 값 0~4/20).
    ※ 다만 **한 번 물어 빗나갔으면 갈래로 좁혀 한 번 더 물어라.** 가장 많은 갈래가
      나머지를 밀어내는 것이라, 좁히면 목록 밖이던 글이 1~4등으로 올라온다
      (얼린 물음 20개 중 셋이 그렇게 살아났다 — `시험기록/실험-갈래로좁히면.py`).
      틀린 갈래면 0건이 오니 손해는 한 번 값이다.
- **2단 `memory/note`** — 고른 것만 펼친다. `heading=` 은 그 토막만,
  `heading=^이름` 은 **블록 한 덩이**만(옵시디언 `[[글#^이름]]` 과 같은 꼴 — 소제목이 없는
  긴 글에서 한 자리를 가리키는 유일한 길이다. 없는 이름이면 **있는 블록 이름**을 돌려준다),
  `q=` 는 **걸린 자리 둘레만**(긴 글에서 41% 감). 자르면 `cut: true` · `full_chars` 로 말한다.
    `q=` 가 없어도 **2만 자에서 자른다** — 흡수는 5만 자짜리 한 줄도 그대로 들이기
    때문이다(원본이 원본이다). `full=1` 로 뚫는다.

쓸 때: `POST /eb/v1/memory` 는 답에 **`link_to`**(뜻으로 가까운 글 세 개)를 붙인다.
**잇기는 선택이다** — 저장소 규칙 1조대로 `[[제목]]` 은 이어짐을 **말하고 싶을 때만** 쓴다.
안 이어도 뜻 검색이 찾는다(오너 창고는 95%가 안 이어져 있는데 20물음 중 13을 찾는다).

제목에 파일 이름으로 못 쓰는 글자(`? : / \ | * " < >`)가 있으면 **모양이 같은 전각**(`？ ： ／ …`)으로
바뀌어 저장되고 답에 `saved_as` 로 알려 준다. 원래 글자로 불러도(`[[질문? 답]]`, `note?title=질문? 답`) 닿는다.

`force`·`pinned` 는 **JSON `true` 일 때만** 참이다. 글자 `"true"`·`"false"`·숫자 `1` 은 거짓으로 본다
(전엔 `"false"` 도 참이 되어 덮어쓰기 막이가 뚫렸다). `kind` 는 글자여야 한다(아니면 400).

1단이 빈손이거나 약할 때는 **왜 그런지** 같이 싣는다(다 괜찮으면 안 싣는다):
`meaning: loading`(켠 뒤 몇 초, 뜻 모델을 올리는 중 — 몇 초 뒤 다시) ·
`meaning: partial`(모델은 올랐는데 벡터가 1할 넘게 비었다 — 색인을 새로 만든 뒤 몇 분) ·
`meaning: none`(뜻 모델이 없어 낱말로만 찾는다).

치우기·고치기:

- `POST /eb/v1/memory/delete` `{title}` — 지운다. **지우기 전에 한 판 남기므로 되돌릴 수 있고**,
  답에 그 자리(`undo`)가 온다. 없는 제목이면 404 에 `did_you_mean`.
- `POST /eb/v1/memory/rename` `{title, to}` — 제목을 고친다. **가리키던 `[[링크]]`도 같이 고친다**
  (옵시디언과 같다). 새 제목이 이미 있으면 409.

`/eb/v1/hello` 는 항목 수 · **갈래별 수** · 태그 · 쓰는 법을 한 번에 준다(오너 창고에서 353자).
이걸 안 보면 AI 는 창고에 무엇이 들었는지 모른 채 헛검색을 한다 — 한 번이 800자다.

항목은 **마크다운 파일이 원본**이고 SQLite는 인덱스다(결정 30). 본문의 `[[제목]]`이 연결이 되며,
검색 결과에는 그 항목의 연결 목록이 함께 실린다(**있을 때만**).
`/eb/v1/graph` 는 **이은 마디만** 준다 — 빈 마디까지 실으면 오너 창고에서 98,425자가
나가는데 그중 쓸모 있는 것이 하나도 없었다(빈 것을 빼니 13자).

제목을 조금 틀리게 주면 404 와 함께 **`did_you_mean`** 으로 가까운 제목을 준다.
없는 소제목에도 있는 소제목 목록을 준다 — **다시 처음부터 찾게 하지 않는다.**

쓴 글은 **바로 뜻으로도 찾힌다**(쓰기 길에서 그 한 장의 벡터를 만든다).
같은 제목으로 또 쓰면 **덧붙는다**(`mode` 기본 `append`). `replace` 로 통째로 덮으려면
**`force: true` 가 있어야 한다** — 없으면 409 다. 덮어쓰기는 되돌리기 어려운 일이라
명시적으로 한다. `edited_by` 만 보고 막던 때가 있었는데, 그 표시는 **화면에서 고칠 때만**
붙어서 **옵시디언·메모장으로 고친 손질이 그냥 지워졌다**(이 물건은 옵시디언 대용이라
밖에서 고치는 것이 주된 길이다).
덮고 나면 답에 **`undo`** 가 실린다 — 지난 판의 자리와 되돌리는 법. 지난 판은 밖에서 고친
글이면 **반드시** 남지만(지문으로 가려낸다), AI 가 그것을 볼 길이 없어 안전망이 반쪽이었다.

폰은 구조화 프로필을 스스로 들고 있고, **깊은 회상만 PC에 묻는다**(결정 22).
PC가 없으면 폰 프로필만으로 동작한다.

검색어 `q`는 **퍼센트 인코딩(UTF-8)** 해서 보낸다. URL에는 ASCII만 실을 수 있다.
같은 이유로 `Authorization` 토큰도 ASCII다(헤더는 latin-1).

### PC → 폰 이벤트 (스킬·개입 제안)

```
GET /eb/v1/events        (Server-Sent Events)
```

PC가 분석해서 만든 제안을 폰으로 밀어 넣는다. **자동 적용은 없다**(결정 18).

```json
{
  "type": "skill_proposal",
  "proposal_id": "01J...",
  "title": "출근 준비",
  "summary": "평일 아침 경로 확인 + 날씨 + 일정 브리핑을 한 번에",
  "based_on": ["01J...", "01J..."],
  "declaration": {}
}
```

`type`은 `skill_proposal` · `skill_update` · `intervention_proposal` 중 하나다.
제안은 즉시 띄우지 않고 **사용자가 한가할 때 모아서** 제시한다(결정 18·23).

### 분석 실행

```
POST /eb/v1/analyze
```

이력을 훑어 제안을 만든다. 같은 제안이 이미 대기 중이면 다시 만들지 않는다.
**자동 적용은 없다** — 만들어서 큐에 넣을 뿐이고, 승인은 아래에서 별도로 받는다(결정 18).

### 제안에 대한 응답

```
POST /eb/v1/proposals/{proposal_id}/decision
```

```json
{ "decision": "approve" }
```

`decision`: `approve` · `reject` · `revert`. `revert`는 이전 버전으로 되돌린다(결정 18).

## 버전 협상

`GET /eb/v1/hello`의 `protocol` 값이 폰이 아는 것보다 높으면 **폰은 자기가 아는 범위로만 쓴다**.
낮으면 없는 기능을 안 부른다. 어느 쪽도 오류가 아니다 — 폰과 PC는 따로 업데이트된다.

## 이 문서가 정하지 않는 것

- 글래스 ↔ 폰 구간. 그쪽은 Android XR SDK가 정하며 이 프로토콜 밖이다.
- 모듈별 요청 형식. 모듈 계약(결정 10)이 따로 정한다.
- 전송 계층 보안. Tailscale이 담당한다(결정 16).
